# TASK-014: Implement recommendTreatment Function

## Function Details
- **Name**: recommendTreatment
- **Category**: Treatment Planning
- **Priority**: Critical
- **Backend Route**: POST `/treatment/recommend` ✅ (Exists)

## Current Implementation
```javascript
async recommendTreatment(params, practiceContext) {
  const response = await this.callAPI('/treatment/recommend', 'POST', params, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Evidence-Based Recommendations
- Clinical practice guidelines
- Treatment protocols by condition
- Medication recommendations with dosing
- Non-pharmacological interventions

### 2. Personalized Treatment Plans
- Patient-specific factors (age, weight, allergies)
- Drug interaction checking
- Contraindication screening
- Alternative treatment options

### 3. Treatment Monitoring
- Follow-up scheduling
- Monitoring parameters
- Treatment effectiveness tracking
- Adverse event monitoring

## Implementation Code
```javascript
async recommendTreatment(params, practiceContext) {\n  try {\n    // Validate required parameters\n    if (!params.diagnosis && !params.condition) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'נדרש אבחנה או מצב רפואי לקבלת המלצת טיפול'\n        : 'Diagnosis or medical condition required for treatment recommendation');\n    }\n    \n    // Prepare treatment request\n    const treatmentRequest = {\n      primaryDiagnosis: params.diagnosis || params.condition,\n      secondaryDiagnoses: params.secondaryDiagnoses || [],\n      patientInfo: {\n        age: params.age || params.patientAge,\n        weight: params.weight || params.patientWeight,\n        gender: params.gender || params.patientGender,\n        pregnancy: params.isPregnant || params.pregnancy,\n        breastfeeding: params.isBreastfeeding || params.breastfeeding\n      },\n      medicalHistory: {\n        allergies: params.allergies || [],\n        currentMedications: params.currentMedications || params.medications || [],\n        chronicConditions: params.chronicConditions || params.comorbidities || [],\n        previousTreatments: params.previousTreatments || [],\n        surgicalHistory: params.surgicalHistory || []\n      },\n      clinicalContext: {\n        severity: params.severity || 'moderate',\n        urgency: params.urgency || 'routine',\n        setting: params.setting || 'outpatient', // outpatient, inpatient, emergency\n        symptoms: params.symptoms || [],\n        vitalSigns: params.vitalSigns || {}\n      },\n      preferences: {\n        treatmentGoals: params.treatmentGoals || [],\n        patientPreferences: params.patientPreferences || [],\n        costConsiderations: params.costConsiderations,\n        includeAlternatives: params.includeAlternatives !== false\n      },\n      options: {\n        includePharmacological: params.includePharmacological !== false,\n        includeNonPharmacological: params.includeNonPharmacological !== false,\n        includeLifestyle: params.includeLifestyle !== false,\n        maxRecommendations: params.maxRecommendations || 5,\n        language: practiceContext.language\n      }\n    };\n    \n    // Get treatment recommendations\n    const response = await this.callAPI(\n      '/treatment/recommend', \n      'POST', \n      treatmentRequest, \n      practiceContext,\n      { timeout: 45000 } // 45 seconds for complex recommendations\n    );\n    \n    const recommendations = response.data;\n    \n    // Process and validate recommendations\n    const processedRecommendations = await this.processRecommendations(\n      recommendations, \n      treatmentRequest, \n      practiceContext\n    );\n    \n    // Check for drug interactions\n    const interactionCheck = await this.checkDrugInteractions(\n      processedRecommendations.pharmacological, \n      treatmentRequest.medicalHistory.currentMedications,\n      practiceContext\n    );\n    \n    // Generate treatment plan\n    const treatmentPlan = this.generateTreatmentPlan(\n      processedRecommendations, \n      treatmentRequest, \n      practiceContext\n    );\n    \n    // Create monitoring plan\n    const monitoringPlan = this.createMonitoringPlan(\n      processedRecommendations, \n      treatmentRequest, \n      practiceContext\n    );\n    \n    // Generate patient education materials\n    const education = await this.generatePatientEducation(\n      processedRecommendations, \n      treatmentRequest, \n      practiceContext\n    );\n    \n    return {\n      success: true,\n      recommendationId: recommendations.id || Date.now().toString(),\n      diagnosis: treatmentRequest.primaryDiagnosis,\n      recommendations: processedRecommendations,\n      treatmentPlan: treatmentPlan,\n      monitoringPlan: monitoringPlan,\n      interactionCheck: interactionCheck,\n      patientEducation: education,\n      confidence: this.calculateRecommendationConfidence(processedRecommendations),\n      evidenceLevel: recommendations.evidenceLevel || 'moderate',\n      message: practiceContext.language === 'he' \n        ? 'המלצות טיפול נוצרו בהצלחה'\n        : 'Treatment recommendations generated successfully',\n      summary: this.generateTreatmentSummary(processedRecommendations, practiceContext),\n      warnings: this.identifyTreatmentWarnings(processedRecommendations, treatmentRequest, practiceContext)\n    };\n    \n  } catch (error) {\n    console.error('Error generating treatment recommendations:', error);\n    return {\n      success: false,\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה ביצירת המלצות טיפול: ${error.message}`\n        : `Error generating treatment recommendations: ${error.message}`\n    };\n  }\n}\n\n// Helper function to process recommendations\nasync processRecommendations(recommendations, treatmentRequest, practiceContext) {\n  const processed = {\n    pharmacological: [],\n    nonPharmacological: [],\n    lifestyle: [],\n    procedures: [],\n    referrals: []\n  };\n  \n  // Process pharmacological treatments\n  if (recommendations.medications) {\n    processed.pharmacological = recommendations.medications.map(med => ({\n      name: med.name || med.medication,\n      genericName: med.genericName,\n      strength: med.strength || med.dose,\n      dosage: med.dosage,\n      frequency: med.frequency,\n      duration: med.duration,\n      route: med.route || 'oral',\n      instructions: med.instructions,\n      indication: med.indication,\n      evidenceLevel: med.evidenceLevel || 'B',\n      alternatives: med.alternatives || [],\n      contraindications: med.contraindications || [],\n      sideEffects: med.sideEffects || [],\n      monitoring: med.monitoring || [],\n      cost: med.cost,\n      priority: med.priority || 'primary'\n    }));\n  }\n  \n  // Process non-pharmacological treatments\n  if (recommendations.nonPharmacological) {\n    processed.nonPharmacological = recommendations.nonPharmacological.map(treatment => ({\n      type: treatment.type,\n      description: treatment.description,\n      instructions: treatment.instructions,\n      frequency: treatment.frequency,\n      duration: treatment.duration,\n      evidenceLevel: treatment.evidenceLevel || 'B',\n      expectedOutcome: treatment.expectedOutcome,\n      contraindications: treatment.contraindications || []\n    }));\n  }\n  \n  // Process lifestyle modifications\n  if (recommendations.lifestyle) {\n    processed.lifestyle = recommendations.lifestyle.map(lifestyle => ({\n      category: lifestyle.category, // diet, exercise, sleep, etc.\n      recommendation: lifestyle.recommendation,\n      rationale: lifestyle.rationale,\n      implementation: lifestyle.implementation,\n      barriers: lifestyle.barriers || [],\n      support: lifestyle.support || []\n    }));\n  }\n  \n  return processed;\n}\n\n// Helper function to check drug interactions\nasync checkDrugInteractions(medications, currentMedications, practiceContext) {\n  if (!medications || !currentMedications || currentMedications.length === 0) {\n    return { hasInteractions: false, interactions: [] };\n  }\n  \n  const allMedications = [\n    ...currentMedications.map(med => typeof med === 'string' ? med : med.name),\n    ...medications.map(med => med.name)\n  ];\n  \n  try {\n    const response = await this.callAPI(\n      '/medications/check-interactions', \n      'POST', \n      { medications: allMedications }, \n      practiceContext\n    );\n    \n    return {\n      hasInteractions: response.data.interactions.length > 0,\n      interactions: response.data.interactions.map(interaction => ({\n        medication1: interaction.drug1,\n        medication2: interaction.drug2,\n        severity: interaction.severity, // mild, moderate, severe\n        description: interaction.description,\n        clinicalSignificance: interaction.clinicalSignificance,\n        management: interaction.management,\n        alternatives: interaction.alternatives || []\n      }))\n    };\n  } catch (error) {\n    console.warn('Failed to check drug interactions:', error);\n    return { hasInteractions: null, error: error.message };\n  }\n}\n\n// Helper function to generate treatment plan\ngenerateTreatmentPlan(recommendations, treatmentRequest, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  \n  return {\n    immediate: this.getImmediateTreatments(recommendations, isHebrew),\n    shortTerm: this.getShortTermTreatments(recommendations, isHebrew),\n    longTerm: this.getLongTermTreatments(recommendations, isHebrew),\n    contingency: this.getContingencyPlans(recommendations, treatmentRequest, isHebrew),\n    goals: this.defineTreatmentGoals(recommendations, treatmentRequest, isHebrew),\n    timeline: this.createTreatmentTimeline(recommendations, isHebrew)\n  };\n}\n\n// Helper function to create monitoring plan\ncreateMonitoringPlan(recommendations, treatmentRequest, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  \n  const monitoringPlan = {\n    parameters: [],\n    schedule: [],\n    alerts: [],\n    followUp: []\n  };\n  \n  // Monitoring parameters for medications\n  recommendations.pharmacological.forEach(med => {\n    if (med.monitoring && med.monitoring.length > 0) {\n      med.monitoring.forEach(param => {\n        monitoringPlan.parameters.push({\n          parameter: param.parameter,\n          medication: med.name,\n          baseline: param.baseline || false,\n          frequency: param.frequency,\n          targetRange: param.targetRange,\n          alertConditions: param.alerts || []\n        });\n      });\n    }\n  });\n  \n  // Follow-up schedule\n  const followUpIntervals = this.calculateFollowUpIntervals(\n    recommendations, \n    treatmentRequest\n  );\n  \n  monitoringPlan.followUp = followUpIntervals.map(interval => ({\n    timepoint: interval.timepoint,\n    purpose: interval.purpose,\n    assessments: interval.assessments,\n    decision_points: interval.decisions\n  }));\n  \n  return monitoringPlan;\n}\n\n// Helper function to generate patient education\nasync generatePatientEducation(recommendations, treatmentRequest, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  \n  const education = {\n    condition: {\n      name: treatmentRequest.primaryDiagnosis,\n      explanation: await this.getConditionExplanation(\n        treatmentRequest.primaryDiagnosis, \n        isHebrew\n      ),\n      prognosis: await this.getPrognosisInfo(\n        treatmentRequest.primaryDiagnosis, \n        isHebrew\n      )\n    },\n    medications: [],\n    lifestyle: [],\n    warning_signs: [],\n    when_to_seek_help: []\n  };\n  \n  // Medication education\n  recommendations.pharmacological.forEach(med => {\n    education.medications.push({\n      name: med.name,\n      purpose: isHebrew ? `מטרת התרופה: ${med.indication}` : `Purpose: ${med.indication}`,\n      instructions: med.instructions,\n      sideEffects: med.sideEffects,\n      precautions: med.contraindications\n    });\n  });\n  \n  // Lifestyle education\n  recommendations.lifestyle.forEach(lifestyle => {\n    education.lifestyle.push({\n      category: lifestyle.category,\n      recommendation: lifestyle.recommendation,\n      rationale: lifestyle.rationale,\n      tips: lifestyle.implementation\n    });\n  });\n  \n  return education;\n}\n\n// Helper function to calculate recommendation confidence\ncalculateRecommendationConfidence(recommendations) {\n  let totalConfidence = 0;\n  let count = 0;\n  \n  // Factor in evidence levels\n  const evidenceScores = { 'A': 1.0, 'B': 0.8, 'C': 0.6, 'D': 0.4 };\n  \n  recommendations.pharmacological.forEach(med => {\n    totalConfidence += evidenceScores[med.evidenceLevel] || 0.6;\n    count++;\n  });\n  \n  recommendations.nonPharmacological.forEach(treatment => {\n    totalConfidence += evidenceScores[treatment.evidenceLevel] || 0.6;\n    count++;\n  });\n  \n  return count > 0 ? totalConfidence / count : 0.6;\n}\n\n// Helper function to identify treatment warnings\nidentifyTreatmentWarnings(recommendations, treatmentRequest, practiceContext) {\n  const warnings = [];\n  const isHebrew = practiceContext.language === 'he';\n  \n  // Check for high-risk medications\n  recommendations.pharmacological.forEach(med => {\n    // Pregnancy warnings\n    if (treatmentRequest.patientInfo.pregnancy && med.contraindications.includes('pregnancy')) {\n      warnings.push({\n        type: 'pregnancy',\n        medication: med.name,\n        severity: 'high',\n        message: isHebrew \n          ? `תרופה ${med.name} לא מומלצת בהריון`\n          : `${med.name} is not recommended during pregnancy`\n      });\n    }\n    \n    // Allergy warnings\n    if (treatmentRequest.medicalHistory.allergies) {\n      const allergyMatch = treatmentRequest.medicalHistory.allergies.find(allergy => \n        med.name.toLowerCase().includes(allergy.toLowerCase()) ||\n        allergy.toLowerCase().includes(med.name.toLowerCase())\n      );\n      \n      if (allergyMatch) {\n        warnings.push({\n          type: 'allergy',\n          medication: med.name,\n          allergy: allergyMatch,\n          severity: 'high',\n          message: isHebrew \n            ? `אלרגיה ידועה לתרופה ${med.name}`\n            : `Known allergy to ${med.name}`\n        });\n      }\n    }\n  });\n  \n  return warnings;\n}\n\n// Additional helper functions\ngetImmediateTreatments(recommendations, isHebrew) {\n  return recommendations.pharmacological\n    .filter(med => med.priority === 'urgent' || med.priority === 'immediate')\n    .map(med => ({\n      treatment: med.name,\n      instructions: med.instructions,\n      timeframe: isHebrew ? 'מיידי' : 'Immediate'\n    }));\n}\n\ncalculateFollowUpIntervals(recommendations, treatmentRequest) {\n  const intervals = [];\n  \n  // Standard follow-ups based on condition severity\n  if (treatmentRequest.clinicalContext.urgency === 'urgent') {\n    intervals.push(\n      { timepoint: '1 week', purpose: 'Response assessment', assessments: ['symptoms', 'side_effects'] },\n      { timepoint: '1 month', purpose: 'Treatment adjustment', assessments: ['effectiveness', 'tolerability'] }\n    );\n  } else {\n    intervals.push(\n      { timepoint: '2 weeks', purpose: 'Initial response', assessments: ['symptoms', 'adherence'] },\n      { timepoint: '6 weeks', purpose: 'Treatment response', assessments: ['effectiveness', 'side_effects'] },\n      { timepoint: '3 months', purpose: 'Long-term assessment', assessments: ['outcomes', 'quality_of_life'] }\n    );\n  }\n  \n  return intervals;\n}\n```\n\n## Testing Checklist\n- [ ] Test treatment recommendations for common conditions\n- [ ] Test drug interaction checking\n- [ ] Test patient-specific factors (age, pregnancy)\n- [ ] Test allergy contraindication checking\n- [ ] Test evidence-based recommendations\n- [ ] Test alternative treatment options\n- [ ] Test monitoring plan generation\n- [ ] Test patient education materials\n- [ ] Test Hebrew treatment instructions\n- [ ] Test English treatment instructions\n\n## Notes\n- Integrate with clinical decision support systems\n- Add support for treatment guidelines databases\n- Implement cost-effectiveness analysis\n- Add patient preference incorporation\n- Consider implementing shared decision-making tools\n- Add support for treatment protocol templates