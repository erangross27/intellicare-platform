# TASK-038: Implement generatePatientReport Function

## Function Details
- **Name**: generatePatientReport
- **Category**: Reports & Analytics
- **Priority**: High
- **Backend Route**: POST `/reports/patient/:id` ✅ (Exists)

## Current Implementation
```javascript
async generatePatientReport(params, practiceContext) {
  const response = await this.callAPI(`/reports/patient/${params.patientId}`, 'POST', params, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Comprehensive Patient Data Collection
- Demographics and contact info
- Medical history and conditions
- Current medications and allergies
- Vital signs trends
- Lab results analysis
- Document summaries

### 2. Report Customization
- Multiple report formats (PDF, HTML, Excel)
- Template selection
- Date range filtering
- Section inclusion/exclusion
- Provider customization

### 3. Clinical Intelligence
- Risk assessments
- Treatment summaries
- Progress tracking
- Recommendations
- Care gaps identification

## Implementation Code
```javascript
async generatePatientReport(params, practiceContext) {\n  try {\n    // Validate parameters\n    if (!params.patientId) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'מזהה מטופל נדרש'\n        : 'Patient ID is required');\n    }\n    \n    // Prepare report configuration\n    const reportConfig = {\n      patientId: params.patientId,\n      reportType: params.reportType || 'comprehensive',\n      format: params.format || 'pdf', // pdf, html, excel, json\n      dateRange: {\n        startDate: params.startDate,\n        endDate: params.endDate || new Date().toISOString()\n      },\n      sections: params.sections || [\n        'demographics', 'medical_history', 'medications', \n        'allergies', 'vital_signs', 'lab_results', \n        'documents', 'assessments'\n      ],\n      includeTrends: params.includeTrends !== false,\n      includeCharts: params.includeCharts !== false,\n      includeAnalysis: params.includeAnalysis !== false,\n      language: practiceContext.language,\n      template: params.template || 'standard'\n    };\n    \n    // Collect patient data\n    const patientData = await this.collectPatientData(\n      reportConfig.patientId, \n      reportConfig.dateRange, \n      practiceContext\n    );\n    \n    if (!patientData.patient) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'מטופל לא נמצא'\n        : 'Patient not found');\n    }\n    \n    // Generate report sections\n    const reportSections = {};\n    \n    for (const sectionName of reportConfig.sections) {\n      const section = await this.generateReportSection(\n        sectionName, \n        patientData, \n        reportConfig, \n        practiceContext\n      );\n      \n      if (section) {\n        reportSections[sectionName] = section;\n      }\n    }\n    \n    // Generate clinical analysis\n    const clinicalAnalysis = await this.generateClinicalAnalysis(\n      patientData, \n      reportConfig, \n      practiceContext\n    );\n    \n    // Create report metadata\n    const reportMetadata = {\n      reportId: Date.now().toString(),\n      generatedDate: new Date().toISOString(),\n      generatedBy: practiceContext.userId || 'agent',\n      practiceId: practiceContext.practiceId,\n      patientId: reportConfig.patientId,\n      patientName: `${patientData.patient.firstName} ${patientData.patient.lastName}`,\n      reportType: reportConfig.reportType,\n      dateRange: reportConfig.dateRange,\n      totalPages: 0, // Will be set after rendering\n      language: practiceContext.language\n    };\n    \n    // Generate the report\n    const response = await this.callAPI(\n      `/reports/patient/${params.patientId}`, \n      'POST', \n      {\n        config: reportConfig,\n        sections: reportSections,\n        analysis: clinicalAnalysis,\n        metadata: reportMetadata\n      }, \n      practiceContext,\n      { timeout: 60000 } // 1 minute for report generation\n    );\n    \n    const generatedReport = response.data;\n    \n    return {\n      success: true,\n      reportId: generatedReport.reportId || reportMetadata.reportId,\n      reportUrl: generatedReport.reportUrl,\n      downloadUrl: generatedReport.downloadUrl,\n      metadata: {\n        ...reportMetadata,\n        totalPages: generatedReport.totalPages,\n        fileSize: generatedReport.fileSize,\n        format: reportConfig.format\n      },\n      summary: this.generateReportSummary(reportSections, clinicalAnalysis, practiceContext),\n      sections: Object.keys(reportSections),\n      analysisHighlights: clinicalAnalysis.highlights || [],\n      message: practiceContext.language === 'he' \n        ? `דוח מטופל נוצר בהצלחה עבור ${patientData.patient.firstName} ${patientData.patient.lastName}`\n        : `Patient report generated successfully for ${patientData.patient.firstName} ${patientData.patient.lastName}`,\n      expirationDate: this.calculateReportExpiration(reportConfig.format)\n    };\n    \n  } catch (error) {\n    console.error('Error generating patient report:', error);\n    return {\n      success: false,\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה ביצירת דוח מטופל: ${error.message}`\n        : `Error generating patient report: ${error.message}`\n    };\n  }\n}\n\n// Helper function to collect patient data\nasync collectPatientData(patientId, dateRange, practiceContext) {\n  const data = {};\n  \n  try {\n    // Get patient demographics\n    const patientResponse = await this.callAPI(\n      `/patients/${patientId}`, \n      'GET', \n      {}, \n      practiceContext\n    );\n    data.patient = patientResponse.data;\n    \n    // Get medical history\n    const historyResponse = await this.callAPI(\n      `/patients/${patientId}/history`, \n      'GET', \n      { \n        startDate: dateRange.startDate, \n        endDate: dateRange.endDate,\n        limit: 100 \n      }, \n      practiceContext\n    );\n    data.medicalHistory = historyResponse.data.history || historyResponse.data || [];\n    \n    // Get medications\n    const medicationsResponse = await this.callAPI(\n      `/patients/${patientId}/medications`, \n      'GET', \n      { active: true }, \n      practiceContext\n    );\n    data.medications = medicationsResponse.data.medications || medicationsResponse.data || [];\n    \n    // Get allergies\n    const allergiesResponse = await this.callAPI(\n      `/patients/${patientId}/allergies`, \n      'GET', \n      {}, \n      practiceContext\n    );\n    data.allergies = allergiesResponse.data.allergies || allergiesResponse.data || [];\n    \n    // Get vital signs\n    const vitalsResponse = await this.callAPI(\n      `/patients/${patientId}/vitals`, \n      'GET', \n      { \n        startDate: dateRange.startDate, \n        endDate: dateRange.endDate,\n        limit: 50 \n      }, \n      practiceContext\n    );\n    data.vitalSigns = vitalsResponse.data.vitals || vitalsResponse.data || [];\n    \n    // Get lab results\n    const labsResponse = await this.callAPI(\n      `/patients/${patientId}/labs`, \n      'GET', \n      { \n        startDate: dateRange.startDate, \n        endDate: dateRange.endDate,\n        limit: 50 \n      }, \n      practiceContext\n    );\n    data.labResults = labsResponse.data.results || labsResponse.data || [];\n    \n    // Get documents\n    const documentsResponse = await this.callAPI(\n      `/patients/${patientId}/documents`, \n      'GET', \n      { \n        startDate: dateRange.startDate, \n        endDate: dateRange.endDate,\n        limit: 20 \n      }, \n      practiceContext\n    );\n    data.documents = documentsResponse.data.documents || documentsResponse.data || [];\n    \n  } catch (error) {\n    console.warn('Error collecting some patient data:', error.message);\n  }\n  \n  return data;\n}\n\n// Helper function to generate report section\nasync generateReportSection(sectionName, patientData, reportConfig, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  \n  switch (sectionName) {\n    case 'demographics':\n      return this.generateDemographicsSection(patientData.patient, isHebrew);\n      \n    case 'medical_history':\n      return this.generateMedicalHistorySection(\n        patientData.medicalHistory, \n        reportConfig.dateRange, \n        isHebrew\n      );\n      \n    case 'medications':\n      return this.generateMedicationsSection(patientData.medications, isHebrew);\n      \n    case 'allergies':\n      return this.generateAllergiesSection(patientData.allergies, isHebrew);\n      \n    case 'vital_signs':\n      return this.generateVitalSignsSection(\n        patientData.vitalSigns, \n        reportConfig.includeTrends, \n        isHebrew\n      );\n      \n    case 'lab_results':\n      return this.generateLabResultsSection(\n        patientData.labResults, \n        reportConfig.includeTrends, \n        isHebrew\n      );\n      \n    case 'documents':\n      return this.generateDocumentsSection(patientData.documents, isHebrew);\n      \n    case 'assessments':\n      return await this.generateAssessmentsSection(\n        patientData, \n        reportConfig, \n        practiceContext\n      );\n      \n    default:\n      return null;\n  }\n}\n\n// Helper function to generate demographics section\ngenerateDemographicsSection(patient, isHebrew) {\n  return {\n    title: isHebrew ? 'פרטים דמוגרפיים' : 'Demographics',\n    content: {\n      personalInfo: {\n        name: `${patient.firstName} ${patient.lastName}`,\n        id: patient.nationalId || patient.id,\n        dateOfBirth: this.formatDate(patient.dateOfBirth, isHebrew),\n        age: this.calculateAge(patient.dateOfBirth),\n        gender: isHebrew ? this.translateGender(patient.gender, true) : patient.gender,\n        maritalStatus: patient.maritalStatus\n      },\n      contactInfo: {\n        phone: patient.phone,\n        email: patient.email,\n        address: this.formatAddress(patient.address, isHebrew),\n        emergencyContact: patient.emergencyContact\n      },\n      insurance: {\n        provider: patient.insuranceProvider,\n        policyNumber: patient.insurancePolicyNumber,\n        groupNumber: patient.insuranceGroupNumber\n      }\n    },\n    charts: [], // No charts for demographics\n    summary: isHebrew \n      ? `${patient.firstName} ${patient.lastName}, גיל ${this.calculateAge(patient.dateOfBirth)}`\n      : `${patient.firstName} ${patient.lastName}, age ${this.calculateAge(patient.dateOfBirth)}`\n  };\n}\n\n// Helper function to generate medical history section\ngenerateMedicalHistorySection(medicalHistory, dateRange, isHebrew) {\n  if (!medicalHistory || medicalHistory.length === 0) {\n    return {\n      title: isHebrew ? 'היסטוריה רפואית' : 'Medical History',\n      content: {\n        message: isHebrew \n          ? 'לא נמצאה היסטוריה רפואית בתקופה הנבחרת'\n          : 'No medical history found in the selected period'\n      },\n      charts: [],\n      summary: isHebrew ? 'אין היסטוריה רפואית' : 'No medical history'\n    };\n  }\n  \n  // Group visits by type and analyze\n  const visitTypes = {};\n  const diagnoses = {};\n  const treatments = {};\n  \n  medicalHistory.forEach(visit => {\n    // Count visit types\n    const type = visit.type || 'general';\n    visitTypes[type] = (visitTypes[type] || 0) + 1;\n    \n    // Count diagnoses\n    if (visit.diagnosis) {\n      diagnoses[visit.diagnosis] = (diagnoses[visit.diagnosis] || 0) + 1;\n    }\n    \n    // Count treatments\n    if (visit.treatment) {\n      treatments[visit.treatment] = (treatments[visit.treatment] || 0) + 1;\n    }\n  });\n  \n  return {\n    title: isHebrew ? 'היסטוריה רפואית' : 'Medical History',\n    content: {\n      totalVisits: medicalHistory.length,\n      visitTypes: visitTypes,\n      commonDiagnoses: Object.entries(diagnoses)\n        .sort(([,a], [,b]) => b - a)\n        .slice(0, 5),\n      commonTreatments: Object.entries(treatments)\n        .sort(([,a], [,b]) => b - a)\n        .slice(0, 5),\n      recentVisits: medicalHistory\n        .slice(0, 10)\n        .map(visit => ({\n          date: this.formatDate(visit.date, isHebrew),\n          complaint: visit.chiefComplaint,\n          diagnosis: visit.diagnosis,\n          treatment: visit.treatment\n        }))\n    },\n    charts: [\n      {\n        type: 'timeline',\n        title: isHebrew ? 'ציר זמן רפואי' : 'Medical Timeline',\n        data: medicalHistory.map(visit => ({\n          date: visit.date,\n          event: visit.diagnosis || visit.chiefComplaint,\n          type: visit.type\n        }))\n      }\n    ],\n    summary: isHebrew \n      ? `${medicalHistory.length} ביקורים רפואיים`\n      : `${medicalHistory.length} medical visits`\n  };\n}\n\n// Helper function to generate clinical analysis\nasync generateClinicalAnalysis(patientData, reportConfig, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  const analysis = {\n    riskAssessment: await this.assessPatientRisks(patientData, practiceContext),\n    careGaps: await this.identifyCareGaps(patientData, practiceContext),\n    trends: await this.analyzeTrends(patientData, practiceContext),\n    recommendations: await this.generateRecommendations(patientData, practiceContext),\n    highlights: []\n  };\n  \n  // Generate highlights based on analysis\n  if (analysis.riskAssessment.highRiskConditions.length > 0) {\n    analysis.highlights.push({\n      type: 'risk',\n      priority: 'high',\n      message: isHebrew \n        ? `זוהו ${analysis.riskAssessment.highRiskConditions.length} גורמי סיכון גבוהים`\n        : `${analysis.riskAssessment.highRiskConditions.length} high-risk conditions identified`\n    });\n  }\n  \n  if (analysis.careGaps.length > 0) {\n    analysis.highlights.push({\n      type: 'care_gap',\n      priority: 'medium',\n      message: isHebrew \n        ? `${analysis.careGaps.length} פערי טיפול זוהו`\n        : `${analysis.careGaps.length} care gaps identified`\n    });\n  }\n  \n  return analysis;\n}\n\n// Helper function to generate report summary\ngenerateReportSummary(sections, analysis, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  const summary = [];\n  \n  // Count sections\n  const sectionCount = Object.keys(sections).length;\n  summary.push(isHebrew \n    ? `${sectionCount} חלקים`\n    : `${sectionCount} sections`\n  );\n  \n  // Medical history summary\n  if (sections.medical_history) {\n    const visits = sections.medical_history.content.totalVisits;\n    summary.push(isHebrew \n      ? `${visits} ביקורים`\n      : `${visits} visits`\n    );\n  }\n  \n  // Medications count\n  if (sections.medications) {\n    const medCount = sections.medications.content.activeMedications?.length || 0;\n    summary.push(isHebrew \n      ? `${medCount} תרופות פעילות`\n      : `${medCount} active medications`\n    );\n  }\n  \n  // Risk assessment\n  if (analysis.riskAssessment?.overallRisk) {\n    const riskLevel = isHebrew \n      ? this.translateRiskLevel(analysis.riskAssessment.overallRisk, true)\n      : analysis.riskAssessment.overallRisk;\n    summary.push(isHebrew \n      ? `סיכון: ${riskLevel}`\n      : `Risk: ${riskLevel}`\n    );\n  }\n  \n  return summary.join(' | ');\n}\n\n// Helper function to calculate report expiration\ncalculateReportExpiration(format) {\n  const expiration = new Date();\n  \n  // Different retention based on format\n  switch (format) {\n    case 'pdf':\n      expiration.setDate(expiration.getDate() + 30); // 30 days\n      break;\n    case 'html':\n      expiration.setDate(expiration.getDate() + 7); // 7 days\n      break;\n    default:\n      expiration.setDate(expiration.getDate() + 14); // 14 days\n  }\n  \n  return expiration.toISOString();\n}\n\n// Additional helper functions\ncalculateAge(birthDate) {\n  const birth = new Date(birthDate);\n  const today = new Date();\n  let age = today.getFullYear() - birth.getFullYear();\n  const monthDiff = today.getMonth() - birth.getMonth();\n  \n  if (monthDiff < 0 || (monthDiff === 0 && today.getDate() < birth.getDate())) {\n    age--;\n  }\n  \n  return age;\n}\n\nformatAddress(address, isHebrew) {\n  if (!address) return '';\n  \n  if (typeof address === 'string') return address;\n  \n  const parts = [];\n  if (address.street) parts.push(address.street);\n  if (address.city) parts.push(address.city);\n  if (address.postalCode) parts.push(address.postalCode);\n  \n  return parts.join(', ');\n}\n\ntranslateGender(gender, isHebrew) {\n  if (!isHebrew) return gender;\n  \n  const translations = {\n    'male': 'זכר',\n    'female': 'נקבה',\n    'other': 'אחר',\n    'unknown': 'לא ידוע'\n  };\n  \n  return translations[gender?.toLowerCase()] || gender;\n}\n\ntranslateRiskLevel(risk, isHebrew) {\n  if (!isHebrew) return risk;\n  \n  const translations = {\n    'low': 'נמוך',\n    'moderate': 'בינוני',\n    'high': 'גבוה',\n    'critical': 'קריטי'\n  };\n  \n  return translations[risk?.toLowerCase()] || risk;\n}\n```\n\n## Testing Checklist\n- [ ] Test comprehensive patient report generation\n- [ ] Test report customization options\n- [ ] Test different report formats (PDF, HTML)\n- [ ] Test date range filtering\n- [ ] Test section inclusion/exclusion\n- [ ] Test clinical analysis generation\n- [ ] Test trend analysis\n- [ ] Test care gap identification\n- [ ] Test Hebrew report generation\n- [ ] Test English report generation\n- [ ] Test report download functionality\n\n## Notes\n- Implement advanced report templates\n- Add support for custom report branding\n- Consider implementing report scheduling\n- Add support for multi-patient reports\n- Implement report sharing capabilities\n- Add support for interactive reports with drill-down capabilities