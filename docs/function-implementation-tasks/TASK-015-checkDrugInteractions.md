# TASK-015: Implement checkDrugInteractions Function

## Function Details
- **Name**: checkDrugInteractions
- **Category**: Medication Safety
- **Priority**: Critical
- **Backend Route**: POST `/medications/interactions` ✅ (Exists)

## Current Implementation
```javascript
async checkDrugInteractions(params, practiceContext) {
  const response = await this.callAPI('/medications/interactions', 'POST', params, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Comprehensive Interaction Checking
- Drug-drug interactions
- Drug-food interactions  
- Drug-disease interactions
- Drug-lab interactions
- Severity classification

### 2. Clinical Decision Support
- Risk assessment
- Management recommendations
- Alternative medications
- Monitoring requirements

### 3. Patient Safety Features
- Real-time alerts
- Contraindication warnings
- Dosage adjustments
- Age-specific considerations

## Implementation Code
```javascript
async checkDrugInteractions(params, practiceContext) {\n  try {\n    // Validate input parameters\n    if (!params.medications || !Array.isArray(params.medications) || params.medications.length < 2) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'נדרשות לפחות 2 תרופות לבדיקת אינטראקציות'\n        : 'At least 2 medications required for interaction checking');\n    }\n    \n    // Normalize medication list\n    const medications = this.normalizeMedications(params.medications);\n    \n    // Prepare interaction check request\n    const checkRequest = {\n      medications: medications,\n      patientInfo: {\n        age: params.age || params.patientAge,\n        weight: params.weight || params.patientWeight,\n        gender: params.gender || params.patientGender,\n        pregnancy: params.isPregnant || params.pregnancy,\n        breastfeeding: params.isBreastfeeding || params.breastfeeding,\n        renalFunction: params.renalFunction,\n        hepaticFunction: params.hepaticFunction\n      },\n      medicalConditions: params.medicalConditions || params.conditions || [],\n      allergies: params.allergies || [],\n      checkTypes: {\n        drugDrug: params.checkDrugDrug !== false,\n        drugFood: params.checkDrugFood !== false,\n        drugDisease: params.checkDrugDisease !== false,\n        drugLab: params.checkDrugLab !== false,\n        contraindications: params.checkContraindications !== false\n      },\n      options: {\n        includeSeverity: true,\n        includeManagement: true,\n        includeAlternatives: params.includeAlternatives !== false,\n        language: practiceContext.language\n      }\n    };\n    \n    // Perform interaction check\n    const response = await this.callAPI(\n      '/medications/interactions', \n      'POST', \n      checkRequest, \n      practiceContext,\n      { timeout: 30000 } // 30 seconds for comprehensive checking\n    );\n    \n    const interactionResults = response.data;\n    \n    // Process and classify interactions\n    const processedInteractions = this.processInteractionResults(\n      interactionResults, \n      checkRequest, \n      practiceContext\n    );\n    \n    // Generate clinical recommendations\n    const recommendations = await this.generateInteractionRecommendations(\n      processedInteractions, \n      checkRequest, \n      practiceContext\n    );\n    \n    // Create monitoring plan for interactions\n    const monitoringPlan = this.createInteractionMonitoringPlan(\n      processedInteractions, \n      practiceContext\n    );\n    \n    // Calculate overall risk score\n    const riskAssessment = this.calculateInteractionRisk(\n      processedInteractions, \n      checkRequest.patientInfo\n    );\n    \n    return {\n      success: true,\n      checkId: interactionResults.checkId || Date.now().toString(),\n      medications: medications,\n      interactions: processedInteractions,\n      recommendations: recommendations,\n      monitoringPlan: monitoringPlan,\n      riskAssessment: riskAssessment,\n      hasSignificantInteractions: this.hasSignificantInteractions(processedInteractions),\n      requiresIntervention: this.requiresIntervention(processedInteractions),\n      message: practiceContext.language === 'he' \n        ? this.generateCheckMessage(processedInteractions, true)\n        : this.generateCheckMessage(processedInteractions, false),\n      summary: this.generateInteractionSummary(processedInteractions, practiceContext),\n      alerts: this.generateInteractionAlerts(processedInteractions, practiceContext)\n    };\n    \n  } catch (error) {\n    console.error('Error checking drug interactions:', error);\n    return {\n      success: false,\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה בבדיקת אינטראקציות תרופתיות: ${error.message}`\n        : `Error checking drug interactions: ${error.message}`\n    };\n  }\n}\n\n// Helper function to normalize medications\nnormalizeMedications(medications) {\n  return medications.map(med => {\n    if (typeof med === 'string') {\n      return {\n        name: med.trim(),\n        genericName: null,\n        strength: null,\n        dosage: null,\n        frequency: null\n      };\n    }\n    \n    return {\n      name: med.name || med.medication,\n      genericName: med.genericName || med.generic,\n      strength: med.strength || med.dose,\n      dosage: med.dosage,\n      frequency: med.frequency,\n      route: med.route || 'oral',\n      indication: med.indication\n    };\n  });\n}\n\n// Helper function to process interaction results\nprocessInteractionResults(interactionResults, checkRequest, practiceContext) {\n  const processed = {\n    drugDrug: [],\n    drugFood: [],\n    drugDisease: [],\n    drugLab: [],\n    contraindications: [],\n    total: 0\n  };\n  \n  // Process drug-drug interactions\n  if (interactionResults.drugDrugInteractions) {\n    processed.drugDrug = interactionResults.drugDrugInteractions.map(interaction => ({\n      id: interaction.id,\n      medication1: interaction.drug1 || interaction.medication1,\n      medication2: interaction.drug2 || interaction.medication2,\n      interactionType: interaction.type,\n      severity: this.standardizeSeverity(interaction.severity),\n      mechanism: interaction.mechanism,\n      effect: interaction.effect,\n      clinicalRelevance: interaction.clinicalRelevance || interaction.significance,\n      documentation: interaction.documentation || 'moderate',\n      onset: interaction.onset,\n      management: interaction.management || [],\n      alternatives: interaction.alternatives || [],\n      monitoring: interaction.monitoring || [],\n      references: interaction.references || []\n    }));\n  }\n  \n  // Process drug-food interactions\n  if (interactionResults.drugFoodInteractions) {\n    processed.drugFood = interactionResults.drugFoodInteractions.map(interaction => ({\n      id: interaction.id,\n      medication: interaction.drug || interaction.medication,\n      food: interaction.food,\n      effect: interaction.effect,\n      severity: this.standardizeSeverity(interaction.severity),\n      recommendation: interaction.recommendation,\n      timing: interaction.timing\n    }));\n  }\n  \n  // Process drug-disease interactions\n  if (interactionResults.drugDiseaseInteractions && checkRequest.medicalConditions.length > 0) {\n    processed.drugDisease = interactionResults.drugDiseaseInteractions.map(interaction => ({\n      id: interaction.id,\n      medication: interaction.drug || interaction.medication,\n      condition: interaction.condition || interaction.disease,\n      effect: interaction.effect,\n      severity: this.standardizeSeverity(interaction.severity),\n      contraindication: interaction.contraindication || false,\n      precaution: interaction.precaution,\n      alternatives: interaction.alternatives || []\n    }));\n  }\n  \n  // Calculate totals\n  processed.total = processed.drugDrug.length + processed.drugFood.length + \n                   processed.drugDisease.length + processed.contraindications.length;\n  \n  return processed;\n}\n\n// Helper function to generate interaction recommendations\nasync generateInteractionRecommendations(interactions, checkRequest, practiceContext) {\n  const recommendations = [];\n  const isHebrew = practiceContext.language === 'he';\n  \n  // High severity drug-drug interactions\n  const highSeverityDrug = interactions.drugDrug.filter(i => \n    i.severity === 'major' || i.severity === 'contraindicated'\n  );\n  \n  for (const interaction of highSeverityDrug) {\n    recommendations.push({\n      type: 'drug_interaction',\n      priority: 'high',\n      medication1: interaction.medication1,\n      medication2: interaction.medication2,\n      action: isHebrew \n        ? 'שקול להפסיק או להחליף אחת מהתרופות'\n        : 'Consider discontinuing or replacing one medication',\n      alternatives: interaction.alternatives,\n      rationale: interaction.effect\n    });\n  }\n  \n  // Moderate severity interactions requiring monitoring\n  const moderateSeverityDrug = interactions.drugDrug.filter(i => i.severity === 'moderate');\n  \n  for (const interaction of moderateSeverityDrug) {\n    if (interaction.monitoring && interaction.monitoring.length > 0) {\n      recommendations.push({\n        type: 'monitoring_required',\n        priority: 'medium',\n        medication1: interaction.medication1,\n        medication2: interaction.medication2,\n        action: isHebrew \n          ? 'נדרש מעקב קליני'\n          : 'Clinical monitoring required',\n        monitoring: interaction.monitoring,\n        frequency: this.determineMonitoringFrequency(interaction.severity)\n      });\n    }\n  }\n  \n  // Drug-disease contraindications\n  const contraindications = interactions.drugDisease.filter(i => i.contraindication);\n  \n  for (const contraindication of contraindications) {\n    recommendations.push({\n      type: 'contraindication',\n      priority: 'high',\n      medication: contraindication.medication,\n      condition: contraindication.condition,\n      action: isHebrew \n        ? 'תרופה זו התווי נגד למצב זה'\n        : 'This medication is contraindicated for this condition',\n      alternatives: contraindication.alternatives\n    });\n  }\n  \n  return recommendations;\n}\n\n// Helper function to create interaction monitoring plan\ncreateInteractionMonitoringPlan(interactions, practiceContext) {\n  const monitoringPlan = {\n    parameters: [],\n    schedule: [],\n    alerts: []\n  };\n  \n  // Monitor parameters for significant drug-drug interactions\n  interactions.drugDrug\n    .filter(i => i.severity === 'major' || i.severity === 'moderate')\n    .forEach(interaction => {\n      if (interaction.monitoring) {\n        interaction.monitoring.forEach(param => {\n          monitoringPlan.parameters.push({\n            parameter: param,\n            medications: [interaction.medication1, interaction.medication2],\n            reason: interaction.effect,\n            frequency: this.determineMonitoringFrequency(interaction.severity),\n            alertThresholds: this.getMonitoringThresholds(param)\n          });\n        });\n      }\n    });\n  \n  // Create monitoring schedule\n  const uniqueParams = [...new Set(monitoringPlan.parameters.map(p => p.parameter))];\n  uniqueParams.forEach(param => {\n    const paramData = monitoringPlan.parameters.find(p => p.parameter === param);\n    monitoringPlan.schedule.push({\n      parameter: param,\n      baseline: true,\n      frequency: paramData.frequency,\n      duration: this.getMonitoringDuration(param)\n    });\n  });\n  \n  return monitoringPlan;\n}\n\n// Helper function to calculate interaction risk\ncalculateInteractionRisk(interactions, patientInfo) {\n  let riskScore = 0;\n  let riskFactors = [];\n  \n  // Weight by severity\n  const severityWeights = {\n    'contraindicated': 10,\n    'major': 7,\n    'moderate': 4,\n    'minor': 1\n  };\n  \n  interactions.drugDrug.forEach(interaction => {\n    riskScore += severityWeights[interaction.severity] || 0;\n  });\n  \n  interactions.drugDisease.forEach(interaction => {\n    if (interaction.contraindication) {\n      riskScore += 10;\n    } else {\n      riskScore += 3;\n    }\n  });\n  \n  // Patient-specific risk factors\n  if (patientInfo.age > 65) {\n    riskScore *= 1.2;\n    riskFactors.push('elderly');\n  }\n  \n  if (patientInfo.age < 18) {\n    riskScore *= 1.1;\n    riskFactors.push('pediatric');\n  }\n  \n  if (patientInfo.pregnancy) {\n    riskScore *= 1.3;\n    riskFactors.push('pregnancy');\n  }\n  \n  if (patientInfo.renalFunction === 'impaired') {\n    riskScore *= 1.2;\n    riskFactors.push('renal_impairment');\n  }\n  \n  if (patientInfo.hepaticFunction === 'impaired') {\n    riskScore *= 1.2;\n    riskFactors.push('hepatic_impairment');\n  }\n  \n  return {\n    score: Math.min(100, Math.round(riskScore)),\n    level: this.categorizeRiskLevel(riskScore),\n    factors: riskFactors,\n    recommendations: this.getRiskBasedRecommendations(riskScore, riskFactors)\n  };\n}\n\n// Helper function to standardize severity\nstandardizeSeverity(severity) {\n  if (!severity) return 'unknown';\n  \n  const sev = severity.toLowerCase();\n  if (['contraindicated', 'avoid', 'do not use'].includes(sev)) return 'contraindicated';\n  if (['major', 'severe', 'serious'].includes(sev)) return 'major';\n  if (['moderate', 'significant'].includes(sev)) return 'moderate';\n  if (['minor', 'mild', 'low'].includes(sev)) return 'minor';\n  \n  return severity;\n}\n\n// Helper function to check if has significant interactions\nhasSignificantInteractions(interactions) {\n  return interactions.drugDrug.some(i => \n    i.severity === 'major' || i.severity === 'contraindicated'\n  ) || interactions.drugDisease.some(i => i.contraindication);\n}\n\n// Helper function to check if requires intervention\nrequiresIntervention(interactions) {\n  return interactions.drugDrug.some(i => \n    i.severity === 'contraindicated' || \n    (i.severity === 'major' && i.clinicalRelevance === 'high')\n  ) || interactions.drugDisease.some(i => i.contraindication);\n}\n\n// Helper function to generate check message\ngenerateCheckMessage(interactions, isHebrew) {\n  const total = interactions.total;\n  const significant = interactions.drugDrug.filter(i => \n    i.severity === 'major' || i.severity === 'contraindicated'\n  ).length;\n  \n  if (total === 0) {\n    return isHebrew \n      ? 'לא נמצאו אינטראקציות משמעותיות בין התרופות'\n      : 'No significant interactions found between medications';\n  }\n  \n  if (significant > 0) {\n    return isHebrew \n      ? `נמצאו ${significant} אינטראקציות משמעותיות מתוך ${total} אינטראקציות כוללות`\n      : `Found ${significant} significant interactions out of ${total} total interactions`;\n  }\n  \n  return isHebrew \n    ? `נמצאו ${total} אינטראקציות - נדרש מעקב`\n    : `Found ${total} interactions - monitoring required`;\n}\n\n// Helper function to generate interaction summary\ngenerateInteractionSummary(interactions, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  const summary = [];\n  \n  // Count by severity\n  const severityCounts = {\n    contraindicated: 0,\n    major: 0,\n    moderate: 0,\n    minor: 0\n  };\n  \n  interactions.drugDrug.forEach(i => {\n    severityCounts[i.severity] = (severityCounts[i.severity] || 0) + 1;\n  });\n  \n  Object.entries(severityCounts).forEach(([severity, count]) => {\n    if (count > 0) {\n      const severityText = this.translateSeverity(severity, isHebrew);\n      summary.push(`${count} ${severityText}`);\n    }\n  });\n  \n  if (interactions.drugFood.length > 0) {\n    summary.push(isHebrew \n      ? `${interactions.drugFood.length} אינטראקציות תרופה-מזון`\n      : `${interactions.drugFood.length} drug-food interactions`\n    );\n  }\n  \n  if (interactions.drugDisease.length > 0) {\n    summary.push(isHebrew \n      ? `${interactions.drugDisease.length} אינטראקציות תרופה-מחלה`\n      : `${interactions.drugDisease.length} drug-disease interactions`\n    );\n  }\n  \n  return summary.join(' | ');\n}\n\n// Additional helper functions\ndetermineMonitoringFrequency(severity) {\n  switch (severity) {\n    case 'contraindicated':\n    case 'major': return 'weekly';\n    case 'moderate': return 'monthly';\n    case 'minor': return 'quarterly';\n    default: return 'monthly';\n  }\n}\n\ncategorizeRiskLevel(score) {\n  if (score >= 30) return 'high';\n  if (score >= 15) return 'moderate';\n  if (score >= 5) return 'low';\n  return 'minimal';\n}\n\ntranslateSeverity(severity, isHebrew) {\n  const translations = {\n    'contraindicated': isHebrew ? 'התווי נגד' : 'contraindicated',\n    'major': isHebrew ? 'חמורות' : 'major',\n    'moderate': isHebrew ? 'בינוניות' : 'moderate',\n    'minor': isHebrew ? 'קלות' : 'minor'\n  };\n  \n  return translations[severity] || severity;\n}\n```\n\n## Testing Checklist\n- [ ] Test basic drug-drug interaction checking\n- [ ] Test severity classification\n- [ ] Test drug-food interactions\n- [ ] Test drug-disease interactions\n- [ ] Test contraindication detection\n- [ ] Test patient-specific risk factors\n- [ ] Test monitoring recommendations\n- [ ] Test alternative medication suggestions\n- [ ] Test Hebrew interaction descriptions\n- [ ] Test English interaction descriptions\n\n## Notes\n- Integrate with comprehensive drug interaction databases (Lexicomp, Micromedex)\n- Add support for drug-herb interactions\n- Implement real-time clinical alerts\n- Add support for interaction mechanism explanations\n- Consider implementing interaction override capabilities with documentation\n- Add support for drug interaction education materials