# TASK-020: Implement addVitalSigns Function

## Function Details
- **Name**: addVitalSigns
- **Category**: Vital Signs
- **Priority**: Critical
- **Backend Route**: POST `/patients/:id/vitals` ✅ (Exists)

## Current Implementation
```javascript
async addVitalSigns(params, practiceContext) {
  const response = await this.callAPI(`/patients/${params.patientId}/vitals`, 'POST', params, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Comprehensive Vital Signs Support
- Blood pressure (systolic/diastolic)
- Heart rate and rhythm
- Temperature (multiple scales)
- Respiratory rate
- Oxygen saturation
- Height and weight (BMI calculation)
- Pain scale assessment

### 2. Clinical Intelligence
- Normal range validation
- Age-appropriate reference ranges
- Critical value alerts
- Trend analysis
- Risk assessment

### 3. Integration Features
- Device integration
- Automated alerts
- Provider notifications
- Patient monitoring

## Implementation Code
```javascript
async addVitalSigns(params, practiceContext) {\n  try {\n    // Validate required parameters\n    if (!params.patientId) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'מזהה מטופל נדרש'\n        : 'Patient ID is required');\n    }\n    \n    if (!this.hasAnyVitalSign(params)) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'נדרש לפחות מדד חיוני אחד'\n        : 'At least one vital sign is required');\n    }\n    \n    // Parse and validate vital signs\n    const vitalSigns = await this.parseVitalSigns(params, practiceContext);\n    \n    // Validate each vital sign\n    const validationResults = await this.validateVitalSigns(\n      vitalSigns, \n      params.patientAge || params.age,\n      practiceContext\n    );\n    \n    if (validationResults.hasErrors) {\n      throw new Error(validationResults.errors.join(', '));\n    }\n    \n    // Prepare vital signs entry\n    const vitalsEntry = {\n      patientId: params.patientId,\n      measurementDate: params.measurementDate || params.date || new Date().toISOString(),\n      \n      // Vital signs measurements\n      bloodPressure: vitalSigns.bloodPressure,\n      heartRate: vitalSigns.heartRate,\n      temperature: vitalSigns.temperature,\n      respiratoryRate: vitalSigns.respiratoryRate,\n      oxygenSaturation: vitalSigns.oxygenSaturation,\n      height: vitalSigns.height,\n      weight: vitalSigns.weight,\n      bmi: vitalSigns.bmi,\n      painScale: vitalSigns.painScale,\n      \n      // Additional measurements\n      glucoseLevel: vitalSigns.glucoseLevel,\n      headCircumference: vitalSigns.headCircumference,\n      \n      // Contextual information\n      position: params.position || 'sitting', // sitting, standing, lying\n      activity: params.activity || 'resting', // resting, post-exercise, etc.\n      location: params.location || 'practice', // practice, home, hospital\n      deviceUsed: params.device || params.deviceUsed,\n      \n      // Assessment information\n      measuredBy: params.measuredBy || practiceContext.userId || 'agent',\n      notes: params.notes || params.comments,\n      \n      // Processing metadata\n      enteredBy: practiceContext.userId || 'agent',\n      entryDate: new Date().toISOString(),\n      practiceId: practiceContext.practiceId,\n      language: practiceContext.language,\n      \n      // Analysis results\n      alerts: [],\n      assessments: [],\n      trends: []\n    };\n    \n    // Analyze vital signs for abnormalities\n    const analysis = await this.analyzeVitalSigns(\n      vitalsEntry, \n      params.patientAge || params.age,\n      params.patientGender || params.gender,\n      practiceContext\n    );\n    \n    vitalsEntry.alerts = analysis.alerts;\n    vitalsEntry.assessments = analysis.assessments;\n    vitalsEntry.overallStatus = analysis.overallStatus;\n    \n    // Save to database\n    const response = await this.callAPI(\n      `/patients/${params.patientId}/vitals`, \n      'POST', \n      vitalsEntry, \n      practiceContext\n    );\n    \n    const savedVitals = response.data;\n    \n    // Generate trend analysis\n    const trendAnalysis = await this.analyzeTrends(\n      params.patientId, \n      vitalsEntry, \n      practiceContext\n    );\n    \n    vitalsEntry.trends = trendAnalysis;\n    \n    // Generate clinical recommendations\n    const recommendations = await this.generateVitalsRecommendations(\n      vitalsEntry, \n      analysis, \n      trendAnalysis, \n      practiceContext\n    );\n    \n    return {\n      success: true,\n      vitalsId: savedVitals._id || savedVitals.id,\n      data: savedVitals,\n      analysis: analysis,\n      trendAnalysis: trendAnalysis,\n      recommendations: recommendations,\n      alertCount: vitalsEntry.alerts.length,\n      criticalAlerts: vitalsEntry.alerts.filter(a => a.severity === 'critical').length,\n      message: practiceContext.language === 'he' \n        ? 'מדדים חיוניים נוספו בהצלחה'\n        : 'Vital signs added successfully',\n      summary: this.generateVitalsSummary(vitalsEntry, analysis, practiceContext)\n    };\n    \n  } catch (error) {\n    console.error('Error adding vital signs:', error);\n    return {\n      success: false,\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה בהוספת מדדים חיוניים: ${error.message}`\n        : `Error adding vital signs: ${error.message}`\n    };\n  }\n}\n\n// Helper function to check if any vital sign is provided\nhasAnyVitalSign(params) {\n  const vitalSignFields = [\n    'bloodPressure', 'systolic', 'diastolic', 'heartRate', 'pulse',\n    'temperature', 'respiratoryRate', 'breathingRate', 'oxygenSaturation',\n    'spo2', 'height', 'weight', 'painScale', 'pain'\n  ];\n  \n  return vitalSignFields.some(field => \n    params[field] !== undefined && params[field] !== null && params[field] !== ''\n  );\n}\n\n// Helper function to parse vital signs\nasync parseVitalSigns(params, practiceContext) {\n  const vitalSigns = {};\n  \n  // Blood pressure parsing\n  if (params.bloodPressure) {\n    vitalSigns.bloodPressure = this.parseBloodPressure(params.bloodPressure);\n  } else if (params.systolic || params.diastolic) {\n    vitalSigns.bloodPressure = {\n      systolic: params.systolic ? parseInt(params.systolic) : null,\n      diastolic: params.diastolic ? parseInt(params.diastolic) : null,\n      unit: 'mmHg'\n    };\n  }\n  \n  // Heart rate\n  if (params.heartRate || params.pulse) {\n    vitalSigns.heartRate = {\n      value: parseInt(params.heartRate || params.pulse),\n      unit: 'bpm',\n      rhythm: params.rhythm || 'regular'\n    };\n  }\n  \n  // Temperature\n  if (params.temperature || params.temp) {\n    vitalSigns.temperature = this.parseTemperature(\n      params.temperature || params.temp,\n      params.temperatureUnit || params.tempUnit\n    );\n  }\n  \n  // Respiratory rate\n  if (params.respiratoryRate || params.breathingRate) {\n    vitalSigns.respiratoryRate = {\n      value: parseInt(params.respiratoryRate || params.breathingRate),\n      unit: 'breaths/min'\n    };\n  }\n  \n  // Oxygen saturation\n  if (params.oxygenSaturation || params.spo2) {\n    vitalSigns.oxygenSaturation = {\n      value: parseFloat(params.oxygenSaturation || params.spo2),\n      unit: '%',\n      roomAir: params.roomAir !== false\n    };\n  }\n  \n  // Height\n  if (params.height) {\n    vitalSigns.height = this.parseHeight(params.height, params.heightUnit);\n  }\n  \n  // Weight\n  if (params.weight) {\n    vitalSigns.weight = this.parseWeight(params.weight, params.weightUnit);\n  }\n  \n  // Calculate BMI if height and weight are available\n  if (vitalSigns.height && vitalSigns.weight) {\n    vitalSigns.bmi = this.calculateBMI(vitalSigns.height, vitalSigns.weight);\n  }\n  \n  // Pain scale\n  if (params.painScale || params.pain) {\n    vitalSigns.painScale = {\n      value: parseInt(params.painScale || params.pain),\n      scale: params.painScaleType || '0-10',\n      location: params.painLocation\n    };\n  }\n  \n  return vitalSigns;\n}\n\n// Helper function to validate vital signs\nasync validateVitalSigns(vitalSigns, patientAge, practiceContext) {\n  const errors = [];\n  const warnings = [];\n  \n  // Blood pressure validation\n  if (vitalSigns.bloodPressure) {\n    const bp = vitalSigns.bloodPressure;\n    if (bp.systolic && (bp.systolic < 50 || bp.systolic > 300)) {\n      errors.push(practiceContext.language === 'he' \n        ? 'לחץ דם סיסטולי לא תקין'\n        : 'Invalid systolic blood pressure');\n    }\n    if (bp.diastolic && (bp.diastolic < 30 || bp.diastolic > 200)) {\n      errors.push(practiceContext.language === 'he' \n        ? 'לחץ דם דיאסטולי לא תקין'\n        : 'Invalid diastolic blood pressure');\n    }\n  }\n  \n  // Heart rate validation\n  if (vitalSigns.heartRate) {\n    const hr = vitalSigns.heartRate.value;\n    if (hr < 20 || hr > 300) {\n      errors.push(practiceContext.language === 'he' \n        ? 'דופק לא תקין'\n        : 'Invalid heart rate');\n    }\n  }\n  \n  // Temperature validation\n  if (vitalSigns.temperature) {\n    const temp = vitalSigns.temperature.celsius;\n    if (temp < 30 || temp > 45) {\n      errors.push(practiceContext.language === 'he' \n        ? 'טמפרטורה לא תקינה'\n        : 'Invalid temperature');\n    }\n  }\n  \n  // Oxygen saturation validation\n  if (vitalSigns.oxygenSaturation) {\n    const spo2 = vitalSigns.oxygenSaturation.value;\n    if (spo2 < 50 || spo2 > 100) {\n      errors.push(practiceContext.language === 'he' \n        ? 'רוויה חמצנית לא תקינה'\n        : 'Invalid oxygen saturation');\n    }\n  }\n  \n  return {\n    hasErrors: errors.length > 0,\n    errors: errors,\n    warnings: warnings\n  };\n}\n\n// Helper function to analyze vital signs\nasync analyzeVitalSigns(vitalsEntry, patientAge, patientGender, practiceContext) {\n  const alerts = [];\n  const assessments = [];\n  const isHebrew = practiceContext.language === 'he';\n  \n  // Analyze blood pressure\n  if (vitalsEntry.bloodPressure) {\n    const bpAnalysis = this.analyzeBP(\n      vitalsEntry.bloodPressure, \n      patientAge, \n      isHebrew\n    );\n    \n    if (bpAnalysis.alerts) {\n      alerts.push(...bpAnalysis.alerts);\n    }\n    \n    assessments.push({\n      vital: 'blood_pressure',\n      status: bpAnalysis.status,\n      category: bpAnalysis.category,\n      recommendation: bpAnalysis.recommendation\n    });\n  }\n  \n  // Analyze heart rate\n  if (vitalsEntry.heartRate) {\n    const hrAnalysis = this.analyzeHeartRate(\n      vitalsEntry.heartRate, \n      patientAge, \n      isHebrew\n    );\n    \n    if (hrAnalysis.alerts) {\n      alerts.push(...hrAnalysis.alerts);\n    }\n    \n    assessments.push({\n      vital: 'heart_rate',\n      status: hrAnalysis.status,\n      recommendation: hrAnalysis.recommendation\n    });\n  }\n  \n  // Analyze temperature\n  if (vitalsEntry.temperature) {\n    const tempAnalysis = this.analyzeTemperature(\n      vitalsEntry.temperature, \n      patientAge, \n      isHebrew\n    );\n    \n    if (tempAnalysis.alerts) {\n      alerts.push(...tempAnalysis.alerts);\n    }\n    \n    assessments.push({\n      vital: 'temperature',\n      status: tempAnalysis.status,\n      recommendation: tempAnalysis.recommendation\n    });\n  }\n  \n  // Determine overall status\n  const overallStatus = this.determineOverallVitalsStatus(alerts, assessments);\n  \n  return {\n    alerts: alerts,\n    assessments: assessments,\n    overallStatus: overallStatus\n  };\n}\n\n// Helper function to analyze blood pressure\nanalyzeBP(bp, patientAge, isHebrew) {\n  const systolic = bp.systolic;\n  const diastolic = bp.diastolic;\n  const alerts = [];\n  \n  let category = 'normal';\n  let status = 'normal';\n  let recommendation = '';\n  \n  // Determine BP category (AHA guidelines)\n  if (systolic >= 180 || diastolic >= 120) {\n    category = 'hypertensive_crisis';\n    status = 'critical';\n    alerts.push({\n      type: 'blood_pressure',\n      severity: 'critical',\n      message: isHebrew \n        ? 'משבר יתר לחץ דם - נדרש טיפול מיידי'\n        : 'Hypertensive crisis - immediate treatment required'\n    });\n    recommendation = isHebrew \n      ? 'הפנייה מיידית לחדר מיון'\n      : 'Immediate referral to emergency department';\n  } else if (systolic >= 140 || diastolic >= 90) {\n    category = 'stage2_hypertension';\n    status = 'abnormal';\n    recommendation = isHebrew \n      ? 'שקול טיפול תרופתי ושינויי אורח חיים'\n      : 'Consider antihypertensive therapy and lifestyle modifications';\n  } else if (systolic >= 130 || diastolic >= 80) {\n    category = 'stage1_hypertension';\n    status = 'borderline';\n    recommendation = isHebrew \n      ? 'שינויי אורח חיים ומעקב'\n      : 'Lifestyle modifications and monitoring';\n  } else if (systolic < 90 || diastolic < 60) {\n    category = 'hypotension';\n    status = 'low';\n    if (systolic < 70 || diastolic < 40) {\n      alerts.push({\n        type: 'blood_pressure',\n        severity: 'high',\n        message: isHebrew \n          ? 'לחץ דם נמוך מאוד'\n          : 'Severe hypotension'\n      });\n    }\n    recommendation = isHebrew \n      ? 'בחינת סיבות ללחץ דם נמוך'\n      : 'Evaluate causes of hypotension';\n  }\n  \n  return {\n    category: category,\n    status: status,\n    alerts: alerts,\n    recommendation: recommendation\n  };\n}\n\n// Helper function to parse blood pressure\nparseBloodPressure(bpString) {\n  if (typeof bpString === 'object') {\n    return bpString;\n  }\n  \n  // Parse \"120/80\" format\n  const match = bpString.match(/(\\d+)\\/(\\d+)/);\n  if (match) {\n    return {\n      systolic: parseInt(match[1]),\n      diastolic: parseInt(match[2]),\n      unit: 'mmHg'\n    };\n  }\n  \n  return null;\n}\n\n// Helper function to parse temperature\nparseTemperature(tempValue, unit) {\n  let celsius;\n  const temp = parseFloat(tempValue);\n  \n  if (!unit || unit.toLowerCase().includes('c')) {\n    celsius = temp;\n  } else if (unit.toLowerCase().includes('f')) {\n    celsius = (temp - 32) * 5/9;\n  } else {\n    // Assume Celsius for values < 50, Fahrenheit otherwise\n    celsius = temp < 50 ? temp : (temp - 32) * 5/9;\n  }\n  \n  return {\n    celsius: Math.round(celsius * 10) / 10,\n    fahrenheit: Math.round((celsius * 9/5 + 32) * 10) / 10,\n    unit: 'C'\n  };\n}\n\n// Helper function to calculate BMI\ncalculateBMI(height, weight) {\n  // Convert to metric if needed\n  let heightM = height.meters || height.value;\n  let weightKg = weight.kg || weight.value;\n  \n  if (height.unit === 'cm') {\n    heightM = height.value / 100;\n  } else if (height.unit === 'in' || height.unit === 'inches') {\n    heightM = height.value * 0.0254;\n  }\n  \n  if (weight.unit === 'lbs' || weight.unit === 'pounds') {\n    weightKg = weight.value * 0.453592;\n  }\n  \n  const bmi = weightKg / (heightM * heightM);\n  \n  return {\n    value: Math.round(bmi * 10) / 10,\n    unit: 'kg/m²',\n    category: this.getBMICategory(bmi)\n  };\n}\n\n// Helper function to generate vitals summary\ngenerateVitalsSummary(vitalsEntry, analysis, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  const summary = [];\n  \n  // Blood pressure\n  if (vitalsEntry.bloodPressure) {\n    const bp = vitalsEntry.bloodPressure;\n    summary.push(`${isHebrew ? 'לד\"מ' : 'BP'}: ${bp.systolic}/${bp.diastolic}`);\n  }\n  \n  // Heart rate\n  if (vitalsEntry.heartRate) {\n    summary.push(`${isHebrew ? 'דופק' : 'HR'}: ${vitalsEntry.heartRate.value}`);\n  }\n  \n  // Temperature\n  if (vitalsEntry.temperature) {\n    summary.push(`${isHebrew ? 'טמפ' : 'Temp'}: ${vitalsEntry.temperature.celsius}°C`);\n  }\n  \n  // Overall status\n  if (analysis.overallStatus !== 'normal') {\n    const statusText = isHebrew ? this.translateStatus(analysis.overallStatus, true) : analysis.overallStatus;\n    summary.push(`${isHebrew ? 'סטטוס' : 'Status'}: ${statusText}`);\n  }\n  \n  return summary.join(' | ');\n}\n\n// Additional helper functions\ngetBMICategory(bmi) {\n  if (bmi < 18.5) return 'underweight';\n  if (bmi < 25) return 'normal';\n  if (bmi < 30) return 'overweight';\n  return 'obese';\n}\n\ndetermineOverallVitalsStatus(alerts, assessments) {\n  if (alerts.some(a => a.severity === 'critical')) {\n    return 'critical';\n  }\n  \n  if (alerts.some(a => a.severity === 'high') || \n      assessments.some(a => a.status === 'abnormal')) {\n    return 'abnormal';\n  }\n  \n  if (assessments.some(a => a.status === 'borderline')) {\n    return 'borderline';\n  }\n  \n  return 'normal';\n}\n```\n\n## Testing Checklist\n- [ ] Test blood pressure entry and analysis\n- [ ] Test heart rate validation\n- [ ] Test temperature conversion\n- [ ] Test BMI calculation\n- [ ] Test critical value alerts\n- [ ] Test age-appropriate ranges\n- [ ] Test trend analysis\n- [ ] Test multiple vital signs entry\n- [ ] Test Hebrew vital signs display\n- [ ] Test English vital signs display\n\n## Notes\n- Integrate with vital signs monitoring devices\n- Add support for pediatric vital signs ranges\n- Implement automated trending algorithms\n- Add support for specialized measurements (PEFR, etc.)\n- Consider implementing vital signs protocols\n- Add support for vital signs graphing and visualization