# TASK-013: Implement analyzeSymptoms Function

## Function Details
- **Name**: analyzeSymptoms
- **Category**: Diagnostic AI
- **Priority**: Critical
- **Backend Route**: POST `/diagnosis/analyze-symptoms` ✅ (Exists)

## Current Implementation
```javascript
async analyzeSymptoms(params, practiceContext) {
  const response = await this.callAPI('/diagnosis/analyze-symptoms', 'POST', params, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Symptom Processing
- Parse natural language symptoms
- Standardize medical terminology
- Map to medical ontologies (SNOMED CT)
- Handle symptom combinations

### 2. AI-Powered Analysis
- Differential diagnosis generation
- Risk stratification
- Urgency assessment
- Red flag identification

### 3. Clinical Decision Support
- Evidence-based recommendations
- Treatment guidelines
- Follow-up recommendations
- Specialist referral suggestions

## Implementation Code
```javascript
async analyzeSymptoms(params, practiceContext) {\n  try {\n    // Validate input parameters\n    if (!params.symptoms && !params.symptomText && !params.complaints) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'נדרש לפחות תסמין אחד לניתוח'\n        : 'At least one symptom is required for analysis');\n    }\n    \n    // Parse and normalize symptoms\n    const normalizedSymptoms = await this.parseSymptoms(params, practiceContext);\n    \n    if (normalizedSymptoms.length === 0) {\n      throw new Error(practiceContext.language === 'he' \n        ? 'לא זוהו תסמינים תקינים לניתוח'\n        : 'No valid symptoms identified for analysis');\n    }\n    \n    // Prepare analysis request\n    const analysisRequest = {\n      symptoms: normalizedSymptoms,\n      patientInfo: {\n        age: params.age || params.patientAge,\n        gender: params.gender || params.patientGender,\n        medicalHistory: params.medicalHistory || [],\n        currentMedications: params.medications || [],\n        allergies: params.allergies || []\n      },\n      analysisOptions: {\n        includeDifferentialDx: params.includeDifferential !== false,\n        includeRiskAssessment: params.includeRisk !== false,\n        includeUrgencyLevel: params.includeUrgency !== false,\n        includeRecommendations: params.includeRecommendations !== false,\n        maxDifferentials: params.maxDifferentials || 5,\n        language: practiceContext.language\n      },\n      context: {\n        chiefComplaint: params.chiefComplaint,\n        duration: params.duration,\n        severity: params.severity,\n        onset: params.onset,\n        triggers: params.triggers,\n        alleviatingFactors: params.alleviatingFactors,\n        associatedSymptoms: params.associatedSymptoms\n      }\n    };\n    \n    // Perform symptom analysis\n    const response = await this.callAPI(\n      '/diagnosis/analyze-symptoms', \n      'POST', \n      analysisRequest, \n      practiceContext,\n      { timeout: 60000 } // 1 minute for complex analysis\n    );\n    \n    const analysisResult = response.data;\n    \n    // Process and enhance results\n    const processedResult = await this.processSymptomAnalysis(\n      analysisResult, \n      normalizedSymptoms, \n      practiceContext\n    );\n    \n    // Generate clinical insights\n    const insights = await this.generateClinicalInsights(\n      processedResult, \n      analysisRequest.patientInfo, \n      practiceContext\n    );\n    \n    // Create comprehensive response\n    const result = {\n      success: true,\n      analysisId: analysisResult.analysisId || Date.now().toString(),\n      symptoms: normalizedSymptoms,\n      analysis: processedResult,\n      insights: insights,\n      confidence: this.calculateAnalysisConfidence(processedResult),\n      urgencyLevel: processedResult.urgencyLevel,\n      riskScore: processedResult.riskScore,\n      message: practiceContext.language === 'he' \n        ? 'ניתוח התסמינים הושלם בהצלחה'\n        : 'Symptom analysis completed successfully',\n      summary: this.generateAnalysisSummary(processedResult, practiceContext),\n      recommendations: processedResult.recommendations || [],\n      followUpRequired: this.assessFollowUpNeed(processedResult)\n    };\n    \n    // Add warnings for high-risk conditions\n    const warnings = this.identifyHighRiskWarnings(processedResult, practiceContext);\n    if (warnings.length > 0) {\n      result.warnings = warnings;\n    }\n    \n    // Log analysis for learning\n    await this.logSymptomAnalysis(analysisRequest, processedResult, practiceContext);\n    \n    return result;\n    \n  } catch (error) {\n    console.error('Error analyzing symptoms:', error);\n    return {\n      success: false,\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה בניתוח תסמינים: ${error.message}`\n        : `Error analyzing symptoms: ${error.message}`\n    };\n  }\n}\n\n// Helper function to parse and normalize symptoms\nasync parseSymptoms(params, practiceContext) {\n  const symptoms = [];\n  \n  // Handle structured symptoms list\n  if (params.symptoms && Array.isArray(params.symptoms)) {\n    for (const symptom of params.symptoms) {\n      if (typeof symptom === 'string') {\n        symptoms.push({\n          name: symptom.trim(),\n          severity: 'moderate',\n          duration: 'unknown'\n        });\n      } else if (typeof symptom === 'object') {\n        symptoms.push({\n          name: symptom.name || symptom.symptom,\n          severity: symptom.severity || 'moderate',\n          duration: symptom.duration,\n          onset: symptom.onset,\n          location: symptom.location,\n          quality: symptom.quality,\n          radiation: symptom.radiation,\n          timing: symptom.timing\n        });\n      }\n    }\n  }\n  \n  // Handle free-text symptom description\n  if (params.symptomText || params.complaints) {\n    const text = params.symptomText || params.complaints;\n    const parsedSymptoms = await this.parseSymptomText(text, practiceContext);\n    symptoms.push(...parsedSymptoms);\n  }\n  \n  // Normalize and validate symptoms\n  const normalizedSymptoms = [];\n  for (const symptom of symptoms) {\n    const normalized = await this.normalizeSymptom(symptom, practiceContext);\n    if (normalized) {\n      normalizedSymptoms.push(normalized);\n    }\n  }\n  \n  return normalizedSymptoms;\n}\n\n// Helper function to parse symptom text\nasync parseSymptomText(text, practiceContext) {\n  const symptoms = [];\n  \n  // Common symptom patterns (English)\n  const englishPatterns = [\n    /\\b(headache|pain in (?:the )?head)\\b/gi,\n    /\\b(chest pain|pain in (?:the )?chest)\\b/gi,\n    /\\b(abdominal pain|stomach pain|belly pain)\\b/gi,\n    /\\b(nausea|feeling sick|queasy)\\b/gi,\n    /\\b(vomiting|throwing up|being sick)\\b/gi,\n    /\\b(fever|high temperature|feeling hot)\\b/gi,\n    /\\b(cough|coughing)\\b/gi,\n    /\\b(shortness of breath|breathing problems|can't breathe)\\b/gi,\n    /\\b(dizziness|feeling dizzy|lightheaded)\\b/gi,\n    /\\b(fatigue|tiredness|feeling tired|exhausted)\\b/gi\n  ];\n  \n  // Common symptom patterns (Hebrew)\n  const hebrewPatterns = [\n    /\\b(כאב ראש|כאב בראש)\\b/g,\n    /\\b(כאב חזה|כאב בחזה)\\b/g,\n    /\\b(כאב בטן|כאב בכרס)\\b/g,\n    /\\b(בחילה|בחילות)\\b/g,\n    /\\b(הקאה|הקאות)\\b/g,\n    /\\b(חום|קדחת)\\b/g,\n    /\\b(שיעול)\\b/g,\n    /\\b(קשיי נשימה|קושי בנשימה)\\b/g,\n    /\\b(סחרחורת|חולשה)\\b/g,\n    /\\b(עייפות|תשישות)\\b/g\n  ];\n  \n  const patterns = practiceContext.language === 'he' ? hebrewPatterns : englishPatterns;\n  const symptomNames = practiceContext.language === 'he' \n    ? ['כאב ראש', 'כאב חזה', 'כאב בטן', 'בחילה', 'הקאה', 'חום', 'שיעול', 'קושי בנשימה', 'סחרחורת', 'עייפות']\n    : ['headache', 'chest pain', 'abdominal pain', 'nausea', 'vomiting', 'fever', 'cough', 'shortness of breath', 'dizziness', 'fatigue'];\n  \n  patterns.forEach((pattern, index) => {\n    if (pattern.test(text)) {\n      symptoms.push({\n        name: symptomNames[index],\n        severity: 'moderate',\n        source: 'parsed_text'\n      });\n    }\n  });\n  \n  return symptoms;\n}\n\n// Helper function to normalize symptom\nasync normalizeSymptom(symptom, practiceContext) {\n  // Map to standardized medical terms\n  const normalizedName = await this.standardizeSymptomName(symptom.name, practiceContext);\n  \n  if (!normalizedName) {\n    return null; // Invalid or unrecognized symptom\n  }\n  \n  return {\n    name: normalizedName,\n    originalName: symptom.name,\n    severity: this.normalizeSeverity(symptom.severity),\n    duration: this.normalizeDuration(symptom.duration),\n    onset: symptom.onset,\n    location: symptom.location,\n    quality: symptom.quality,\n    snomedCode: await this.getSnomedCode(normalizedName),\n    validated: true\n  };\n}\n\n// Helper function to process symptom analysis\nasync processSymptomAnalysis(analysisResult, symptoms, practiceContext) {\n  return {\n    differentialDiagnosis: this.processDifferentialDx(analysisResult.differentialDx, practiceContext),\n    primaryDiagnosis: analysisResult.primaryDiagnosis,\n    urgencyLevel: this.categorizeUrgency(analysisResult.urgencyScore),\n    riskScore: analysisResult.riskScore || 0,\n    riskFactors: analysisResult.riskFactors || [],\n    redFlags: this.identifyRedFlags(analysisResult, symptoms),\n    recommendations: this.formatRecommendations(analysisResult.recommendations, practiceContext),\n    workupSuggested: analysisResult.suggestedWorkup || [],\n    specialistReferral: analysisResult.referralSuggestions || [],\n    followUpTimeframe: analysisResult.followUpTimeframe,\n    confidenceScore: analysisResult.confidence || 0.5\n  };\n}\n\n// Helper function to process differential diagnosis\nprocessDifferentialDx(differentialDx, practiceContext) {\n  if (!differentialDx || !Array.isArray(differentialDx)) {\n    return [];\n  }\n  \n  return differentialDx.map(dx => ({\n    diagnosis: dx.diagnosis,\n    probability: dx.probability || dx.confidence,\n    reasoning: dx.reasoning,\n    supportingSymptoms: dx.supportingSymptoms || [],\n    contradictingFactors: dx.contradictingFactors || [],\n    icdCode: dx.icdCode,\n    urgency: this.categorizeDiagnosisUrgency(dx),\n    workupNeeded: dx.recommendedWorkup || []\n  })).sort((a, b) => b.probability - a.probability);\n}\n\n// Helper function to identify red flags\nidentifyRedFlags(analysisResult, symptoms) {\n  const redFlags = [];\n  \n  // Check for emergency symptoms\n  const emergencySymptoms = ['chest pain', 'difficulty breathing', 'severe headache', \n                           'loss of consciousness', 'severe abdominal pain'];\n  \n  symptoms.forEach(symptom => {\n    if (emergencySymptoms.some(emergency => \n      symptom.name.toLowerCase().includes(emergency.toLowerCase()))) {\n      redFlags.push({\n        type: 'emergency_symptom',\n        symptom: symptom.name,\n        severity: 'high'\n      });\n    }\n  });\n  \n  // Check diagnosis-specific red flags\n  if (analysisResult.differentialDx) {\n    analysisResult.differentialDx.forEach(dx => {\n      if (dx.urgency === 'emergency' || dx.probability > 0.8) {\n        redFlags.push({\n          type: 'high_risk_diagnosis',\n          diagnosis: dx.diagnosis,\n          probability: dx.probability,\n          severity: 'high'\n        });\n      }\n    });\n  }\n  \n  return redFlags;\n}\n\n// Helper function to categorize urgency\ncategorizeUrgency(urgencyScore) {\n  if (urgencyScore >= 0.8) return 'emergency';\n  if (urgencyScore >= 0.6) return 'urgent';\n  if (urgencyScore >= 0.4) return 'semi_urgent';\n  return 'routine';\n}\n\n// Helper function to generate clinical insights\nasync generateClinicalInsights(processedResult, patientInfo, practiceContext) {\n  const insights = [];\n  const isHebrew = practiceContext.language === 'he';\n  \n  // Age-specific insights\n  if (patientInfo.age) {\n    if (patientInfo.age < 18) {\n      insights.push({\n        type: 'age_specific',\n        message: isHebrew \n          ? 'חשוב לשקול אבחנות ספציפיות לגיל הילדות'\n          : 'Consider pediatric-specific diagnoses'\n      });\n    } else if (patientInfo.age > 65) {\n      insights.push({\n        type: 'age_specific',\n        message: isHebrew \n          ? 'יש להתחשב באבחנות שכיחות בגיל המתקדם'\n          : 'Consider age-related conditions common in elderly patients'\n      });\n    }\n  }\n  \n  // Gender-specific insights\n  if (patientInfo.gender && processedResult.differentialDx) {\n    const genderSpecific = processedResult.differentialDx.filter(dx => \n      this.isGenderSpecificCondition(dx.diagnosis, patientInfo.gender)\n    );\n    \n    if (genderSpecific.length > 0) {\n      insights.push({\n        type: 'gender_specific',\n        message: isHebrew \n          ? 'ישנן אבחנות ספציפיות למין הרלוונטיות'\n          : 'Gender-specific conditions are relevant'\n      });\n    }\n  }\n  \n  // Risk level insights\n  if (processedResult.riskScore > 0.7) {\n    insights.push({\n      type: 'high_risk',\n      severity: 'warning',\n      message: isHebrew \n        ? 'מצב עם רמת סיכון גבוהה - נדרשת הערכה דחופה'\n        : 'High-risk condition - urgent evaluation required'\n    });\n  }\n  \n  return insights;\n}\n\n// Helper function to calculate analysis confidence\ncalculateAnalysisConfidence(processedResult) {\n  let confidence = 0;\n  let factors = 0;\n  \n  // Differential diagnosis confidence\n  if (processedResult.differentialDx && processedResult.differentialDx.length > 0) {\n    const avgConfidence = processedResult.differentialDx\n      .reduce((sum, dx) => sum + dx.probability, 0) / processedResult.differentialDx.length;\n    confidence += avgConfidence * 0.4;\n  }\n  factors += 0.4;\n  \n  // Symptom clarity\n  if (processedResult.confidenceScore) {\n    confidence += processedResult.confidenceScore * 0.3;\n  }\n  factors += 0.3;\n  \n  // Red flags presence (reduces confidence if present)\n  if (processedResult.redFlags && processedResult.redFlags.length > 0) {\n    confidence += 0.1;\n  } else {\n    confidence += 0.3;\n  }\n  factors += 0.3;\n  \n  return Math.min(1, confidence / factors);\n}\n\n// Additional helper functions\nnormalizeSeverity(severity) {\n  if (!severity) return 'moderate';\n  const sev = severity.toLowerCase();\n  if (['mild', 'light', 'slight'].includes(sev)) return 'mild';\n  if (['moderate', 'medium'].includes(sev)) return 'moderate';\n  if (['severe', 'bad', 'intense'].includes(sev)) return 'severe';\n  return 'moderate';\n}\n\nnormalizeDuration(duration) {\n  if (!duration) return 'unknown';\n  const dur = duration.toLowerCase();\n  if (dur.includes('hour') || dur.includes('שעה')) return 'hours';\n  if (dur.includes('day') || dur.includes('יום')) return 'days';\n  if (dur.includes('week') || dur.includes('שבוע')) return 'weeks';\n  if (dur.includes('month') || dur.includes('חודש')) return 'months';\n  return duration;\n}\n\nstandardizeSymptomName(name, practiceContext) {\n  // This would integrate with medical terminology databases\n  const standardTerms = {\n    // English terms\n    'headache': 'headache',\n    'head pain': 'headache',\n    'chest pain': 'chest pain',\n    'difficulty breathing': 'dyspnea',\n    'shortness of breath': 'dyspnea',\n    \n    // Hebrew terms\n    'כאב ראש': 'headache',\n    'כאב בראש': 'headache',\n    'כאב חזה': 'chest pain',\n    'קושי בנשימה': 'dyspnea',\n    'קשיי נשימה': 'dyspnea'\n  };\n  \n  return standardTerms[name.toLowerCase()] || name;\n}\n```\n\n## Testing Checklist\n- [ ] Test single symptom analysis\n- [ ] Test multiple symptom analysis\n- [ ] Test symptom text parsing\n- [ ] Test differential diagnosis generation\n- [ ] Test urgency assessment\n- [ ] Test red flag identification\n- [ ] Test age-specific considerations\n- [ ] Test gender-specific conditions\n- [ ] Test Hebrew symptom parsing\n- [ ] Test English symptom parsing\n- [ ] Test confidence scoring\n\n## Notes\n- Integrate with medical knowledge bases (UpToDate, BMJ)\n- Implement SNOMED CT terminology mapping\n- Add support for symptom severity scales\n- Consider implementing symptom duration tracking\n- Add integration with clinical decision support systems\n- Implement machine learning models for pattern recognition