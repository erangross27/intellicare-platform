# Gitleaks configuration for IntelliCare.
# Historical false positives are fingerprinted in .gitleaksignore so this file
# never exempts an entire source directory from future secret detection.
[extend]
useDefault = true
