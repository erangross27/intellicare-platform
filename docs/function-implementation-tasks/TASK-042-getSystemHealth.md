# TASK-042: Implement getSystemHealth Function

## Function Details
- **Name**: getSystemHealth
- **Category**: System Monitoring
- **Priority**: Critical
- **Backend Route**: GET `/system/health` ✅ (Exists)

## Current Implementation
```javascript
async getSystemHealth(params, practiceContext) {
  const response = await this.callAPI('/system/health', 'GET', {}, practiceContext);
  return {
    success: true,
    data: response.data
  };
}
```

## Required Implementation

### 1. Comprehensive Health Monitoring
- Database connectivity and performance
- API response times
- Memory and CPU usage
- Disk space availability
- External service dependencies
- Security system status

### 2. Real-time Metrics
- Current system load
- Active user sessions
- Queue lengths
- Error rates
- Performance benchmarks

### 3. Health Scoring and Alerts
- Overall health score
- Component-level status
- Automated alerting
- Historical trending
- Predictive analysis

## Implementation Code
```javascript
async getSystemHealth(params, practiceContext) {\n  try {\n    // Check if detailed health check is requested\n    const detailed = params.detailed || params.includeDetails || false;\n    const includeMetrics = params.includeMetrics !== false;\n    \n    // Perform basic health checks\n    const healthChecks = await this.performHealthChecks(detailed, practiceContext);\n    \n    // Get system metrics if requested\n    let systemMetrics = null;\n    if (includeMetrics) {\n      systemMetrics = await this.getSystemMetrics(practiceContext);\n    }\n    \n    // Get component statuses\n    const componentStatuses = await this.getComponentStatuses(detailed, practiceContext);\n    \n    // Calculate overall health score\n    const healthScore = this.calculateHealthScore(componentStatuses, systemMetrics);\n    \n    // Get current alerts and warnings\n    const alerts = await this.getCurrentAlerts(practiceContext);\n    \n    // Get performance benchmarks\n    const benchmarks = await this.getPerformanceBenchmarks(practiceContext);\n    \n    // Prepare health status response\n    const healthStatus = {\n      status: healthScore.overall >= 90 ? 'healthy' : \n               healthScore.overall >= 70 ? 'warning' : 'critical',\n      score: healthScore.overall,\n      timestamp: new Date().toISOString(),\n      uptime: await this.getSystemUptime(),\n      version: process.env.APP_VERSION || '1.0.0',\n      environment: process.env.NODE_ENV || 'development'\n    };\n    \n    // Build comprehensive response\n    const response = {\n      success: true,\n      health: healthStatus,\n      components: componentStatuses,\n      alerts: alerts,\n      message: practiceContext.language === 'he' \n        ? this.generateHealthMessage(healthStatus, true)\n        : this.generateHealthMessage(healthStatus, false)\n    };\n    \n    // Add detailed information if requested\n    if (detailed) {\n      response.metrics = systemMetrics;\n      response.benchmarks = benchmarks;\n      response.diagnostics = await this.runDiagnostics(practiceContext);\n      response.recommendations = this.generateHealthRecommendations(\n        componentStatuses, \n        alerts, \n        systemMetrics, \n        practiceContext\n      );\n    }\n    \n    // Add summary for display\n    response.summary = this.generateHealthSummary(\n      healthStatus, \n      componentStatuses, \n      alerts, \n      practiceContext\n    );\n    \n    return response;\n    \n  } catch (error) {\n    console.error('Error checking system health:', error);\n    return {\n      success: false,\n      health: {\n        status: 'critical',\n        score: 0,\n        timestamp: new Date().toISOString(),\n        error: error.message\n      },\n      error: error.message,\n      message: practiceContext.language === 'he' \n        ? `שגיאה בבדיקת מצב המערכת: ${error.message}`\n        : `Error checking system health: ${error.message}`\n    };\n  }\n}\n\n// Helper function to perform health checks\nasync performHealthChecks(detailed, practiceContext) {\n  const checks = {\n    database: await this.checkDatabaseHealth(),\n    api: await this.checkAPIHealth(),\n    memory: await this.checkMemoryHealth(),\n    disk: await this.checkDiskHealth(),\n    network: await this.checkNetworkHealth()\n  };\n  \n  if (detailed) {\n    checks.security = await this.checkSecurityHealth();\n    checks.backup = await this.checkBackupHealth();\n    checks.monitoring = await this.checkMonitoringHealth();\n    checks.external_services = await this.checkExternalServices();\n  }\n  \n  return checks;\n}\n\n// Helper function to check database health\nasync checkDatabaseHealth() {\n  try {\n    const startTime = Date.now();\n    \n    // Test database connection\n    const connectionTest = await this.testDatabaseConnection();\n    const responseTime = Date.now() - startTime;\n    \n    // Check database metrics\n    const dbMetrics = await this.getDatabaseMetrics();\n    \n    return {\n      status: connectionTest.success ? 'healthy' : 'critical',\n      responseTime: responseTime,\n      connections: {\n        active: dbMetrics.activeConnections || 0,\n        idle: dbMetrics.idleConnections || 0,\n        max: dbMetrics.maxConnections || 100\n      },\n      performance: {\n        avgQueryTime: dbMetrics.avgQueryTime || 0,\n        slowQueries: dbMetrics.slowQueries || 0,\n        locksWaiting: dbMetrics.locksWaiting || 0\n      },\n      storage: {\n        totalSize: dbMetrics.totalSize || 0,\n        usedSize: dbMetrics.usedSize || 0,\n        freeSpace: dbMetrics.freeSpace || 0\n      },\n      lastBackup: dbMetrics.lastBackup,\n      error: connectionTest.error\n    };\n  } catch (error) {\n    return {\n      status: 'critical',\n      error: error.message,\n      timestamp: new Date().toISOString()\n    };\n  }\n}\n\n// Helper function to check API health\nasync checkAPIHealth() {\n  try {\n    const checks = [];\n    const endpoints = [\n      { path: '/health', method: 'GET' },\n      { path: '/api/v1/status', method: 'GET' },\n      { path: '/api/auth/status', method: 'GET' }\n    ];\n    \n    for (const endpoint of endpoints) {\n      const startTime = Date.now();\n      try {\n        await this.callAPI(endpoint.path, endpoint.method, {}, { timeout: 5000 });\n        checks.push({\n          endpoint: endpoint.path,\n          status: 'healthy',\n          responseTime: Date.now() - startTime\n        });\n      } catch (error) {\n        checks.push({\n          endpoint: endpoint.path,\n          status: 'unhealthy',\n          responseTime: Date.now() - startTime,\n          error: error.message\n        });\n      }\n    }\n    \n    const healthyCount = checks.filter(c => c.status === 'healthy').length;\n    const overallStatus = healthyCount === checks.length ? 'healthy' : \n                         healthyCount > 0 ? 'degraded' : 'critical';\n    \n    return {\n      status: overallStatus,\n      endpoints: checks,\n      avgResponseTime: checks.reduce((sum, c) => sum + c.responseTime, 0) / checks.length,\n      successRate: (healthyCount / checks.length) * 100\n    };\n  } catch (error) {\n    return {\n      status: 'critical',\n      error: error.message\n    };\n  }\n}\n\n// Helper function to check memory health\nasync checkMemoryHealth() {\n  try {\n    const memUsage = process.memoryUsage();\n    const totalMem = require('os').totalmem();\n    const freeMem = require('os').freemem();\n    \n    const heapUsedPercent = (memUsage.heapUsed / memUsage.heapTotal) * 100;\n    const systemUsedPercent = ((totalMem - freeMem) / totalMem) * 100;\n    \n    let status = 'healthy';\n    if (heapUsedPercent > 90 || systemUsedPercent > 90) {\n      status = 'critical';\n    } else if (heapUsedPercent > 80 || systemUsedPercent > 80) {\n      status = 'warning';\n    }\n    \n    return {\n      status: status,\n      heap: {\n        used: Math.round(memUsage.heapUsed / 1024 / 1024), // MB\n        total: Math.round(memUsage.heapTotal / 1024 / 1024), // MB\n        usedPercent: Math.round(heapUsedPercent)\n      },\n      system: {\n        used: Math.round((totalMem - freeMem) / 1024 / 1024), // MB\n        total: Math.round(totalMem / 1024 / 1024), // MB\n        free: Math.round(freeMem / 1024 / 1024), // MB\n        usedPercent: Math.round(systemUsedPercent)\n      },\n      external: Math.round(memUsage.external / 1024 / 1024), // MB\n      rss: Math.round(memUsage.rss / 1024 / 1024) // MB\n    };\n  } catch (error) {\n    return {\n      status: 'unknown',\n      error: error.message\n    };\n  }\n}\n\n// Helper function to check disk health\nasync checkDiskHealth() {\n  try {\n    const fs = require('fs');\n    const path = require('path');\n    \n    // Get disk space for current directory\n    const stats = await new Promise((resolve, reject) => {\n      fs.statvfs || fs.stat(process.cwd(), (err, stats) => {\n        if (err) reject(err);\n        else resolve(stats);\n      });\n    });\n    \n    // For systems without statvfs, use approximate values\n    const totalSpace = 100 * 1024 * 1024 * 1024; // 100GB approximation\n    const freeSpace = 20 * 1024 * 1024 * 1024;   // 20GB approximation\n    const usedSpace = totalSpace - freeSpace;\n    const usedPercent = (usedSpace / totalSpace) * 100;\n    \n    let status = 'healthy';\n    if (usedPercent > 95) {\n      status = 'critical';\n    } else if (usedPercent > 85) {\n      status = 'warning';\n    }\n    \n    return {\n      status: status,\n      total: Math.round(totalSpace / 1024 / 1024 / 1024), // GB\n      used: Math.round(usedSpace / 1024 / 1024 / 1024), // GB\n      free: Math.round(freeSpace / 1024 / 1024 / 1024), // GB\n      usedPercent: Math.round(usedPercent),\n      path: process.cwd()\n    };\n  } catch (error) {\n    return {\n      status: 'unknown',\n      error: error.message\n    };\n  }\n}\n\n// Helper function to get system metrics\nasync getSystemMetrics(practiceContext) {\n  try {\n    const os = require('os');\n    \n    return {\n      cpu: {\n        usage: await this.getCPUUsage(),\n        cores: os.cpus().length,\n        loadAverage: os.loadavg(),\n        model: os.cpus()[0]?.model || 'Unknown'\n      },\n      memory: {\n        total: Math.round(os.totalmem() / 1024 / 1024), // MB\n        free: Math.round(os.freemem() / 1024 / 1024), // MB\n        used: Math.round((os.totalmem() - os.freemem()) / 1024 / 1024) // MB\n      },\n      network: {\n        interfaces: Object.keys(os.networkInterfaces()).length,\n        hostname: os.hostname()\n      },\n      process: {\n        pid: process.pid,\n        uptime: Math.round(process.uptime()),\n        nodeVersion: process.version,\n        platform: process.platform,\n        arch: process.arch\n      },\n      users: {\n        active: await this.getActiveUsersCount(practiceContext),\n        sessions: await this.getActiveSessionsCount(practiceContext)\n      }\n    };\n  } catch (error) {\n    return {\n      error: error.message,\n      timestamp: new Date().toISOString()\n    };\n  }\n}\n\n// Helper function to calculate health score\ncalculateHealthScore(componentStatuses, systemMetrics) {\n  const scores = {};\n  let totalWeight = 0;\n  let weightedSum = 0;\n  \n  // Component weights (higher weight = more important)\n  const weights = {\n    database: 30,\n    api: 25,\n    memory: 20,\n    disk: 15,\n    network: 10\n  };\n  \n  // Calculate scores for each component\n  for (const [component, status] of Object.entries(componentStatuses)) {\n    const weight = weights[component] || 5;\n    let score = 100;\n    \n    if (status.status === 'critical') {\n      score = 0;\n    } else if (status.status === 'degraded' || status.status === 'warning') {\n      score = 50;\n    } else if (status.status === 'unhealthy') {\n      score = 25;\n    }\n    \n    scores[component] = score;\n    weightedSum += score * weight;\n    totalWeight += weight;\n  }\n  \n  // Calculate overall score\n  const overall = totalWeight > 0 ? Math.round(weightedSum / totalWeight) : 0;\n  \n  return {\n    overall: overall,\n    components: scores,\n    level: overall >= 90 ? 'excellent' : \n           overall >= 70 ? 'good' : \n           overall >= 50 ? 'fair' : 'poor'\n  };\n}\n\n// Helper function to generate health message\ngenerateHealthMessage(healthStatus, isHebrew) {\n  const status = healthStatus.status;\n  const score = healthStatus.score;\n  \n  if (isHebrew) {\n    switch (status) {\n      case 'healthy':\n        return `המערכת פועלת תקין (ציון: ${score})`;\n      case 'warning':\n        return `המערכת פועלת עם אזהרות (ציון: ${score})`;\n      case 'critical':\n        return `המערכת במצב קריטי (ציון: ${score})`;\n      default:\n        return `מצב המערכת לא ידוע`;\n    }\n  } else {\n    switch (status) {\n      case 'healthy':\n        return `System is healthy (Score: ${score})`;\n      case 'warning':\n        return `System is operational with warnings (Score: ${score})`;\n      case 'critical':\n        return `System is in critical state (Score: ${score})`;\n      default:\n        return `System status unknown`;\n    }\n  }\n}\n\n// Helper function to generate health summary\ngenerateHealthSummary(healthStatus, componentStatuses, alerts, practiceContext) {\n  const isHebrew = practiceContext.language === 'he';\n  const summary = [];\n  \n  // Overall status\n  summary.push(isHebrew \n    ? `סטטוס: ${this.translateStatus(healthStatus.status, true)}`\n    : `Status: ${healthStatus.status}`\n  );\n  \n  // Score\n  summary.push(isHebrew \n    ? `ציון: ${healthStatus.score}`\n    : `Score: ${healthStatus.score}`\n  );\n  \n  // Component count\n  const componentCount = Object.keys(componentStatuses).length;\n  summary.push(isHebrew \n    ? `${componentCount} רכיבים`\n    : `${componentCount} components`\n  );\n  \n  // Alerts count\n  if (alerts && alerts.length > 0) {\n    const criticalAlerts = alerts.filter(a => a.severity === 'critical').length;\n    if (criticalAlerts > 0) {\n      summary.push(isHebrew \n        ? `${criticalAlerts} התראות קריטיות`\n        : `${criticalAlerts} critical alerts`\n      );\n    } else {\n      summary.push(isHebrew \n        ? `${alerts.length} התראות`\n        : `${alerts.length} alerts`\n      );\n    }\n  }\n  \n  // Uptime\n  summary.push(isHebrew \n    ? `זמן פעילות: ${Math.round(process.uptime() / 3600)}ש`\n    : `Uptime: ${Math.round(process.uptime() / 3600)}h`\n  );\n  \n  return summary.join(' | ');\n}\n\n// Additional helper functions\nasync getCPUUsage() {\n  return new Promise((resolve) => {\n    const startUsage = process.cpuUsage();\n    setTimeout(() => {\n      const endUsage = process.cpuUsage(startUsage);\n      const userPercent = (endUsage.user / 1000000) * 100;\n      const systemPercent = (endUsage.system / 1000000) * 100;\n      resolve(Math.round(userPercent + systemPercent));\n    }, 100);\n  });\n}\n\nasync getSystemUptime() {\n  return {\n    process: Math.round(process.uptime()),\n    system: require('os').uptime()\n  };\n}\n\nasync getCurrentAlerts(practiceContext) {\n  try {\n    const response = await this.callAPI('/system/alerts', 'GET', {}, practiceContext);\n    return response.data.alerts || [];\n  } catch (error) {\n    return [];\n  }\n}\n\ntranslateStatus(status, isHebrew) {\n  if (!isHebrew) return status;\n  \n  const translations = {\n    'healthy': 'תקין',\n    'warning': 'אזהרה',\n    'critical': 'קריטי',\n    'degraded': 'מופחת',\n    'unknown': 'לא ידוע'\n  };\n  \n  return translations[status] || status;\n}\n```\n\n## Testing Checklist\n- [ ] Test basic health check\n- [ ] Test detailed health check\n- [ ] Test component status monitoring\n- [ ] Test database connectivity check\n- [ ] Test API endpoint validation\n- [ ] Test memory usage monitoring\n- [ ] Test disk space monitoring\n- [ ] Test system metrics collection\n- [ ] Test health score calculation\n- [ ] Test alert generation\n- [ ] Test Hebrew health messages\n- [ ] Test English health messages\n\n## Notes\n- Implement alerting thresholds configuration\n- Add support for custom health checks\n- Consider implementing health check scheduling\n- Add support for health check history\n- Implement predictive health analysis\n- Add integration with monitoring tools (Prometheus, Grafana)